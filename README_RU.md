# ArenaApp - Медиаплеер

## Описание проекта

ArenaApp - это простое WPF-приложение для воспроизведения видеофайлов. Приложение предоставляет базовый интерфейс для загрузки и управления воспроизведением медиафайлов.

## Функциональность

- **Загрузка медиафайлов**: Поддержка различных форматов видео (MP4, AVI, MOV, WMV, FLV, MKV), изображений (JPG, PNG, BMP, GIF) и аудио (MP3, WAV, FLAC, AAC)
- **Управление воспроизведением**: 
  - Воспроизведение
  - Пауза
  - Остановка
  - Таймер обратного отсчета (показывает оставшееся время воспроизведения)
  - Слайдеры перемотки для видео и аудио (позволяют быстро перематывать медиафайлы)
  - Интеллектуальная замена типов медиа (звук заменяет звук, видео заменяет видео, картинка заменяет картинку)
  - Умная защита от дублирования (аудио защищены от дублирования, изображения и видео заменяются)
  - Интеллектуальная логика триггеров (аудио продолжает играть при замене визуального контента)
  - Правильная замена аудио между слотами и триггерами (аудио из триггеров заменяет аудио из слотов)
- **Система проектов**: 
  - Создание, открытие и сохранение проектов в JSON формате
  - Привязка медиафайлов к слотам (кнопкам)
  - Визуальные превью для разных типов медиа
- **Интерактивные слоты**: 20 основных слотов (10x2) + 10 кнопок-триггеров для параллельного воспроизведения
- **Простой интерфейс**: Интуитивно понятные кнопки управления и меню

## Технические детали

- **Платформа**: .NET 8.0
- **UI Framework**: WPF (Windows Presentation Foundation)
- **Язык**: C#
- **Архитектура**: MVVM-подобная структура с code-behind

## Структура проекта

```
ArenaApp/
├── App.xaml                 # Главный файл приложения
├── App.xaml.cs             # Код приложения
├── MainWindow.xaml         # Главное окно (UI)
├── MainWindow.xaml.cs      # Логика главного окна
└── ArenaApp.csproj         # Файл проекта
```

## Установка и запуск

1. Убедитесь, что установлен .NET 8.0 SDK
2. Клонируйте репозиторий
3. Откройте проект в Visual Studio или используйте командную строку:
   ```bash
   dotnet build
   dotnet run
   ```

## Использование

### Основные функции
1. Запустите приложение
2. Нажмите кнопку "Загрузить" для выбора медиафайла
3. Используйте кнопки управления:
   - **Воспроизвести**: Начать воспроизведение
   - **Остановить**: Поставить на паузу
   - **Закрыть**: Остановить и очистить медиаэлемент
4. Используйте слайдеры перемотки:
   - **Слайдер видео**: Расположен под текстом "Видео: HH:MM:SS", позволяет перематывать видео
   - **Слайдер аудио**: Расположен под текстом "Аудио: HH:MM:SS", позволяет перематывать аудио
   - Перетащите ползунок слайдера для быстрого перехода к нужному моменту
   - Слайдеры автоматически обновляются во время воспроизведения

### Логика замены типов медиа
Приложение использует интеллектуальную систему замены медиа:

**🔄 Замена одинаковых типов:**
- Звук заменяет звук (новое аудио останавливает предыдущее)
- Видео заменяет видео (новое видео останавливает предыдущее)
- Картинка заменяет картинку (новое изображение заменяет предыдущее)

**✅ Параллельные комбинации:**
- Звук + картинка (аудио + изображение одновременно)
- Звук + видео (аудио + видео одновременно)
- Видео + аудио (через триггеры)

**🎯 Принцип работы:**
- Любой тип медиа может заменить любой другой тип
- Нет блокирующих сообщений - все работает интуитивно
- Предотвращение багов через правильную замену контента

### Умная защита от дублирования медиафайлов
Приложение использует селективную защиту в зависимости от типа медиа:

**🔒 Защита от дублирования (только аудио):**
- Аудио файлы защищены от дублирования - если аудио уже воспроизводится, показывается предупреждение
- Предотвращение багов с параллельным воспроизведением одного аудио файла

**🔄 Замена файлов (изображения и видео):**
- Изображения и видео могут заменять друг друга - если файл уже воспроизводится, он заменяется
- Интуитивное поведение: визуальный контент свободно заменяется

**💡 Примеры:**
- Запустили триггер (звук1+картинка1) → попытка запустить звук1 отдельно → предупреждение (аудио защищено)
- Запустили триггер (звук2+картинка1) → попытка запустить триггер2 с картинкой1 → картинка заменяется (изображение заменяется)
- Запустили видео1 в слоте → попытка запустить видео1 в триггере → видео заменяется (видео заменяется)

### Интеллектуальная логика триггеров
Триггеры работают с умной заменой контента:

**🎯 Умная замена для триггеров:**
- При запуске нового триггера аудио продолжает играть, если оно уже воспроизводится
- Визуальный контент (изображения/видео) заменяется на новый
- Нет прерывания аудио при переключении между триггерами

**💡 Примеры работы:**
- Запустили триггер (морген+книга) → попытка запустить триггер2 (тетрадь+морген) → книга заменяется на тетрадь, морген продолжает играть
- Запустили триггер (кока+видео1) → попытка запустить триггер2 (кока+видео2) → видео1 заменяется на видео2, кока продолжает играть
- Запустили триггер (звук1+картинка1) → попытка запустить триггер2 (звук2+картинка1) → картинка остается, звук1 заменяется на звук2

### Правильная замена аудио между слотами и триггерами
Аудио корректно заменяется между разными источниками:

**🔄 Замена аудио:**
- Аудио из триггеров заменяет аудио из отдельных слотов
- Аудио из отдельных слотов заменяет аудио из триггеров
- Нет параллельного воспроизведения разных аудио файлов

**💡 Примеры замены:**
- Играет кока в отдельном слоте → запуск триггера2 (тетрадь+морген) → кока останавливается, морген начинает играть
- Играет триггер (морген+книга) → запуск аудио кока в отдельном слоте → морген останавливается, кока начинает играть
- Играет триггер (звук1+картинка1) → запуск триггера2 (звук2+картинка2) → звук1 заменяется на звук2, картинка1 заменяется на картинка2

### Работа с проектами
1. **Создание проекта**: File → New Project
2. **Загрузка медиа в слоты**: Нажмите на любую кнопку "Пусто" в нижней панели
3. **Создание текстовых блоков**: При клике на пустой слот выберите кнопку "Текст" в диалоге для создания текстового блока
4. **Воспроизведение из слота**: Нажмите на слот с медиа (отображается иконкой 🎥🖼️🎵T)
5. **Параллельное воспроизведение**: Нажмите на оранжевую кнопку ▶ под колонкой для одновременного воспроизведения медиа из строк 1 и 2
6. **Контекстное меню**: Правый клик по любому слоту или триггеру открывает меню с функциями:
   - **Заново**: Перезапуск воспроизведения медиа
   - **Пауза**: Постановка на паузу или возобновление воспроизведения
   - **Удалить**: Удаление медиа из слота (выделено красным цветом)
7. **Сохранение проекта**: File → Save Project (выберите место для JSON файла)
8. **Открытие проекта**: File → Open Project (выберите JSON файл проекта)

### Кнопки-триггеры (▶)
- **Расположение**: Третья строка под каждой колонкой
- **Размер**: В 2 раза меньше обычных слотов (35x35 px)
- **Функция**: Одновременное воспроизведение комбинаций:
  - Видео + Аудио (видео в плеере + аудио параллельно)
  - Изображение + Аудио (изображение в плеере + аудио параллельно)
  - Если только один тип медиа - обычное воспроизведение

#### Состояния кнопок-триггеров:
- **▶ (Оранжевый)**: Остановлено - нажмите для запуска
- **⏹ (Красный)**: Воспроизводится - нажмите для паузы  
- **⏸ (Желтый)**: На паузе - нажмите для возобновления

#### Автоматическая остановка:
- Воспроизведение продолжается до окончания самого длинного медиафайла
- При окончании любого медиа - автоматический возврат к состоянию "Остановлено"
- Все временные элементы автоматически очищаются

### Типы медиафайлов
- **Видео**: MP4, AVI, MOV, WMV, FLV, MKV (отображается как 🎥)
- **Изображения**: JPG, JPEG, PNG, BMP, GIF (отображается как 🖼️)
- **Аудио**: MP3, WAV, FLAC, AAC (отображается как 🎵)
- **Текстовые блоки**: Пользовательский текст (отображается как T)

### Текстовые блоки

**Создание текстовых блоков:**
- При клике на пустой слот появляется диалог выбора типа контента
- Выберите кнопку "Текст" для создания текстового блока
- Введите желаемый текст в диалоговом окне
- Текстовый блок сохраняется в проекте и отображается в слоте с иконкой "T"

**Отображение текста:**
- Текстовые блоки отображаются в медиаплеере поверх видео или изображений
- Текст центрируется и автоматически переносится по строкам
- Поддерживается вывод на второй экран с синхронизацией
- Текст отображается с настраиваемыми параметрами (шрифт, размер, цвет)

**Настройки текстовых блоков:**
- **Шрифт**: Arial по умолчанию
- **Размер шрифта**: 24px по умолчанию
- **Цвет текста**: Белый по умолчанию
- **Цвет фона**: Прозрачный по умолчанию
- **Положение текста**: 9 предустановленных позиций (TopLeft, TopCenter, TopRight, CenterLeft, Center, CenterRight, BottomLeft, BottomCenter, BottomRight)
- **Ручная настройка положения**: Возможность задать точные координаты X и Y
- Все настройки сохраняются в JSON файле проекта

**Использование:**
- Текстовые блоки можно использовать для отображения заголовков, подписей, объявлений
- Идеально подходят для презентаций, мероприятий, информационных экранов
- **Параллельное воспроизведение**: Текстовые блоки НЕ заменяют видео/фото, а отображаются поверх них (как аудио)
- Поддерживают многострочный текст с автоматическим переносом
- Можно комбинировать с любыми медиафайлами для создания сложных презентаций

### Панель настроек

Панель настроек находится слева от медиаплеера и состоит из двух частей:

#### Настройки элемента (левая панель)
Позволяет настроить индивидуальные параметры для выбранного элемента:

1. **Скорость воспроизведения** - слайдер от 0.1x до 10x с предустановленными кнопками (0.5x, 2x, 3x, 5x, 10x)
2. **Управление воспроизведением** - кнопки воспроизведения, остановки и перезапуска для выбранного элемента
3. **Прозрачность** - слайдер от 0% до 100%
4. **Уровень звука** - слайдер от 0% до 100% с предустановленными кнопками (10%, 25%, 50%, 75%, 100%)
5. **Переименование элемента** - кнопка для изменения отображаемого имени элемента

#### Общие настройки проекта (правая панель)
Позволяет настроить глобальные параметры, которые имеют приоритет над индивидуальными настройками:

1. **Общая громкость** - глобальная настройка громкости для всех элементов (приоритет над личными настройками)
2. **Общая прозрачность** - глобальная настройка прозрачности для всех элементов (приоритет над личными настройками)
3. **Переходы между медиа** - настройка типа и длительности переходов между элементами:
   - Мгновенный переход
   - Плавное затухание
   - Скольжение
   - Увеличение/уменьшение
4. **Дополнительные настройки**:
   - Автоматическое воспроизведение следующего элемента
   - Зацикливание плейлиста

#### Принцип работы общих настроек
- **Приоритет общих настроек**: Если включена общая громкость на 50%, то все элементы будут воспроизводиться с громкостью 50%, независимо от их личных настроек
- **Сохранение личных настроек**: Личные настройки элементов не изменяются - они просто игнорируются при включенных общих настройках
- **Гибкость**: Можно отключить общие настройки и вернуться к использованию индивидуальных параметров каждого элемента

#### Переходы между медиа элементами
- **Типы переходов**:
  - **Мгновенный**: Медиа меняется без анимации (по умолчанию)
  - **Плавное затухание**: Медиа плавно исчезает и появляется
  - **Скольжение**: Медиа "выезжает" слева и "въезжает" справа
  - **Увеличение/уменьшение**: Медиа уменьшается до 50% и увеличивается обратно
- **Длительность переходов**: Настраивается от 0.1 до 3.0 секунд
- **Применение**: Переходы применяются при смене видео и изображений в основном плеере

#### Автопереход на следующий элемент
- **Логика работы**: При окончании воспроизведения медиа автоматически запускается следующий элемент в той же строке
- **Поиск следующего**: Система ищет следующий элемент справа от текущего в той же строке
- **Зацикливание**: При включенной опции "Зацикливание плейлиста" после последнего элемента в строке воспроизводится первый
- **Поддержка**: Работает для обычных слотов (триггеры пока не поддерживаются)

#### Сохранение настроек
Все настройки (как индивидуальные, так и общие) сохраняются в JSON файл проекта и автоматически применяются при загрузке проекта.

## История изменений

### [2024-12-19] - Рефакторинг: Перенос логики LoadMediaFromSlotSelective в MediaPlayerService

**Рефакторинг:**
- **Перенос LoadMediaFromSlotSelective в сервис**: Перенесен метод LoadMediaFromSlotSelective (~700 строк) из MainWindow.xaml.cs в MediaPlayerService для улучшения архитектуры и разделения ответственности
- **Разделение логики**: Логика загрузки медиа разделена на отдельные методы (LoadVideoFromSlot, LoadImageFromSlot, LoadAudioFromSlot, LoadTextFromSlot) для лучшей организации кода
- **Использование делегатов**: Настроены делегаты для доступа к UI элементам и другим сервисам, что позволяет сервису работать с UI без прямых зависимостей
- **Интеграция с существующими сервисами**: MediaPlayerService теперь использует MediaStateService, TransitionService, SettingsManager и DeviceManager для выполнения своих задач
- **Асинхронная обработка**: Метод LoadMediaFromSlotSelective теперь асинхронный, что улучшает производительность при работе с переходами

**Техническая реализация:**
- Расширен MediaPlayerService с добавлением метода LoadMediaFromSlotSelective и вспомогательных методов
- Настроены делегаты в MainWindow для связи сервиса с UI элементами
- Метод в MainWindow теперь является оберткой, вызывающей метод из сервиса
- Сохранена обратная совместимость - все существующие вызовы метода продолжают работать

**Улучшения:**
- Код стал более модульным и легче тестировать
- Улучшена читаемость и поддержка кода
- Подготовлена почва для дальнейшего рефакторинга других методов

### [2024-12-19] - Перенос корректного вывода на проектор из LowBackUp

**Перенесенный функционал:**
- **Корректный вывод на большой проектор**: Перенесена реализация корректного вывода медиа на проектор из версии LowBackUp, где проблема с отображением была исправлена
- **Режим масштабирования**: Добавлена переменная `_useUniformToFill` для выбора режима масштабирования (заполнить весь экран или сохранить пропорции)
- **Чекбокс выбора режима**: Добавлен чекбокс "Заполнить весь экран (может обрезать изображение)" в диалог выбора экрана для настройки режима масштабирования
- **Улучшенное создание окна проектора**: Обновлен метод `CreateSecondaryScreenWindow()`:
  - Добавлены `AllowsTransparency = false` и `ResizeMode = ResizeMode.NoResize` для стабильной работы
  - Изменено масштабирование MediaElement с жестко заданного `UniformToFill` на динамическое с использованием `_useUniformToFill`
  - Изменено выравнивание с `Center` на `Stretch` для заполнения всего экрана
  - Добавлен `Margin = new Thickness(0)` для убирания всех отступов
  - Улучшен обработчик `Loaded` для принудительной установки позиции и размера окна
- **Синхронизация изображений**: Обновлено создание изображений для второго экрана с использованием выбранного режима масштабирования и правильными настройками выравнивания

**Технические детали:**
- Медиа теперь корректно отображается во весь экран проектора без обрезания и искажений (при режиме "Сохранить пропорции")
- Видео и изображения масштабируются правильно с сохранением пропорций или заполнением экрана (в зависимости от выбранного режима)
- Полное использование экрана проектора без черных полос по краям (при режиме "Заполнить экран")
- Стабильная работа с проекторами различных разрешений

### [2024-12-19] - Исправление критического бага с настройками масштаба и поворота

**Исправленные проблемы:**
- **Баг с потерей настроек масштаба и поворота**: ОКОНЧАТЕЛЬНО исправлена критическая проблема, когда настройки масштаба и поворота элементов не применялись или сбрасывались при изменении других настроек (например, прозрачности). Проблема была в методах переходов между медиа, которые неправильно сохраняли и восстанавливали RenderTransform, теряя при этом настройки масштаба и поворота.
- **Баг с применением в реальном времени**: Исправлена проблема, когда настройки масштаба и поворота не применялись в реальном времени при перетаскивании слайдеров. Теперь изменения видны сразу при движении слайдера, а не только после отпускания.
- **Баг с неправильной точкой поворота**: Исправлена проблема, когда поворот медиа элементов происходил от неправильной точки (центра элемента вместо центра медиаплеера), что приводило к смещению при повороте. Теперь поворот происходит от центра медиаплеера.

**Техническая реализация:**
- **Исправлены методы переходов**: Переработаны все методы переходов (ApplySlideTransition, ApplySlideTransitionWithSecondaryScreen, ApplyZoomTransition, ApplyZoomTransitionWithSecondaryScreen) для правильного объединения существующих трансформаций с новыми
- **Умное объединение TransformGroup**: Теперь при создании анимаций переходов система сохраняет существующие ScaleTransform и RotateTransform, исключая только те трансформации, которые используются в анимации (TranslateTransform для скольжения, ScaleTransform для зума)
- **Правильное восстановление трансформаций**: После завершения анимаций система восстанавливает только нужные трансформации, сохраняя настройки масштаба и поворота пользователя
- **Применение в реальном времени**: Улучшен метод ApplyElementSettings() для применения масштаба и поворота к главному плееру, изображениям и текстовым блокам в реальном времени
- **Правильная точка поворота**: Изменена логика применения трансформаций - теперь масштаб и поворот применяются к mediaBorder вместо отдельных элементов, что обеспечивает поворот от центра медиаплеера
- **Синхронизация с вторым экраном**: Исправления применены как к основному, так и к дополнительному экрану для полной синхронизации

**Пользовательский опыт:**
- **ПРОБЛЕМА РЕШЕНА**: Настройки масштаба и поворота теперь работают стабильно и не сбрасываются при изменении других настроек
- **Применение в реальном времени**: Изменения масштаба и поворота видны сразу при перетаскивании слайдеров, без необходимости отпускать слайдер
- **Правильная точка поворота**: Поворот медиа элементов теперь происходит от центра медиаплеера, без смещения при повороте на 180° или другие углы
- **Корректная работа переходов**: Все типы переходов (затухание, скольжение, зум) теперь сохраняют настройки масштаба и поворота
- **Стабильная работа настроек**: Больше не нужно "дергать" настройки прозрачности для применения масштаба и поворота
- **Синхронизация между экранами**: Настройки масштаба и поворота корректно применяются на основном и дополнительном экранах

### [2024-12-19] - Исправление бага с применением общих настроек при загрузке проекта

**Исправленная проблема:**
- **Баг с общей прозрачностью при загрузке проекта**: Исправлена проблема, когда при загрузке проекта с сохраненными общими настройками прозрачности (включенной с значением 0) настройки не применялись к активным медиа элементам. Теперь при загрузке проекта все общие настройки (прозрачность, громкость, масштаб, поворот) корректно применяются к активным медиа элементам.

**Техническая реализация:**
- **Добавлен вызов ApplyGlobalSettings()**: В метод `LoadGlobalSettings()` добавлен вызов `ApplyGlobalSettings()` в конце для применения общих настроек к активным медиа элементам после загрузки проекта
- **Дополнительное принудительное применение**: В метод `LoadMediaFromSlotSelective()` добавлено принудительное применение общих настроек прозрачности к основному медиа элементу после загрузки медиа
- **Принудительное применение при загрузке медиа**: Добавлено принудительное применение прозрачности сразу после установки `_currentMainMedia` в case для видео и изображений в `LoadMediaFromSlotSelective()`
- **Добавлены отладочные сообщения**: Добавлены отладочные сообщения в `ApplyGlobalSettings()`, `GetFinalOpacity()` и `LoadGlobalSettings()` для диагностики проблем с применением настроек
- **Исправлена последовательность применения**: Теперь общие настройки применяются не только при изменении в UI, но и при загрузке проекта из файла и при загрузке медиа
- **Улучшена стабильность**: Все общие настройки (прозрачность, громкость, масштаб, поворот) теперь корректно применяются при загрузке проекта и медиа

**Пользовательский опыт:**
- **ПРОБЛЕМА РЕШЕНА**: При загрузке проекта с сохраненными общими настройками прозрачности (включенной с значением 0) прозрачность теперь корректно применяется к активным медиа элементам
- **Корректное применение всех общих настроек**: При загрузке проекта все общие настройки (прозрачность, громкость, масштаб, поворот) применяются к активным медиа элементам
- **Стабильная работа**: Больше не нужно "дергать" настройки для их применения - они применяются автоматически при загрузке проекта

### [2024-12-19] - Исправление критических багов с настройками масштаба, поворота и прозрачности

**Исправленные проблемы:**
- **Прозрачность не применялась при загрузке**: Исправлена проблема, когда сохраненные настройки прозрачности не применялись при загрузке медиа элементов
- **Масштаб "съедал" медиа**: Исправлена критическая проблема, когда изменение масштаба приводило к исчезновению медиа и невозможности воспроизведения
- **Поворот "съедал" медиа**: Исправлена критическая проблема, когда изменение поворота приводило к исчезновению медиа и невозможности воспроизведения

**Техническая реализация:**
- **Добавлен вызов ApplyGlobalSettings()**: В метод `LoadMediaFromSlotSelective` добавлен вызов `ApplyGlobalSettings()` после загрузки медиа для применения сохраненных настроек
- **Реализовано применение Transform**: Добавлено применение `ScaleTransform` и `RotateTransform` через `TransformGroup` к медиа элементам
- **Обновлены методы ApplyElementSettings**: Добавлено применение масштаба и поворота к главному плееру, активным медиа элементам, изображениям и текстовым блокам
- **Обновлен метод ApplyGlobalSettings**: Добавлено применение масштаба и поворота ко всем типам медиа элементов с учетом общих настроек
- **Синхронизация с вторым экраном**: Добавлена синхронизация масштаба и поворота между основным и дополнительным экранами

**Пользовательский опыт:**
- **Корректное применение настроек**: Теперь все настройки (прозрачность, масштаб, поворот) корректно применяются при загрузке медиа
- **Стабильная работа масштаба**: Масштаб от 10% до 500% работает стабильно без "съедания" медиа
- **Стабильная работа поворота**: Поворот от -360° до +360° работает стабильно без "съедания" медиа
- **Сохранение состояния**: Все настройки корректно сохраняются и восстанавливаются при переключении между медиа элементами

### [2024-12-19] - Расширение до 20 колонок и добавление настроек масштаба и поворота

**Новые функции:**
- **20 колонок вместо 10**: Увеличено количество колонок для медиа элементов с 10 до 20, что позволяет разместить больше медиафайлов в проекте
- **Настройки масштаба (Scale)**: Добавлена возможность изменения масштаба медиа элементов от 10% до 500%
- **Настройки поворота (Rotation)**: Добавлена возможность поворота медиа элементов от -360° до +360°
- **Общие настройки масштаба и поворота**: Добавлены глобальные настройки масштаба и поворота с приоритетом над личными настройками элементов

**Техническая реализация:**
- **Обновлена модель данных**: Добавлены поля `Scale` и `Rotation` в `MediaSlot`, а также `GlobalScale`, `UseGlobalScale`, `GlobalRotation`, `UseGlobalRotation` в `GlobalSettings`
- **Расширен UI**: Добавлены новые GroupBox элементы с слайдерами и предустановленными кнопками для масштаба и поворота
- **Обработчики событий**: Реализованы обработчики для всех новых элементов управления (слайдеры, чекбоксы, кнопки предустановок)
- **Методы применения настроек**: Добавлены методы `GetFinalScale()` и `GetFinalRotation()` для учета общих настроек
- **Обновлена загрузка настроек**: Методы `LoadElementSettings()` и `LoadGlobalSettings()` обновлены для работы с новыми полями

**Пользовательский опыт:**
- **Больше места для медиа**: Теперь доступно 20 колонок вместо 10 для размещения медиафайлов
- **Гибкие настройки масштаба**: Возможность приближать/уменьшать медиа от 10% до 500% с предустановленными значениями (25%, 50%, 75%, 100%, 150%, 200%)
- **Полный контроль поворота**: Поворот медиа элементов на любой угол от -360° до +360° с предустановленными углами (0°, 90°, 180°, 270°, -90°, -180°)
- **Общие настройки**: Возможность применить масштаб и поворот ко всем элементам проекта одновременно
- **Приоритет общих настроек**: Общие настройки масштаба и поворота имеют приоритет над личными настройками элементов
- **Автосохранение**: Все новые настройки автоматически сохраняются в проект

### [2024-12-19] - Исправление бага с общей прозрачностью

**Исправленная проблема:**
- **Баг с общей прозрачностью**: ОКОНЧАТЕЛЬНО исправлена проблема, когда функция общей прозрачности не работала. Теперь при включении общей прозрачности и изменении её значения все активные медиа элементы корректно применяют общую прозрачность.

**Техническая реализация:**
- **Улучшен метод `ApplyGlobalSettings()`**: Добавлено корректное применение общей прозрачности ко всем типам медиа элементов (видео, изображения, текстовые блоки, аудио)
- **Исправлено применение прозрачности по типам**: Для изображений прозрачность применяется к `mediaBorder`, для видео - к `mediaElement`, для текстовых блоков - к `textOverlayGrid`
- **Добавлена синхронизация с вторым экраном**: Общая прозрачность корректно применяется к дополнительному экрану
- **Улучшены обработчики настроек**: При изменении общей прозрачности также вызывается `ApplyElementSettings()` для выбранного элемента
- **Добавлена отладочная информация**: Добавлены отладочные сообщения для диагностики применения прозрачности

**Пользовательский опыт:**
- **ПРОБЛЕМА РЕШЕНА**: Общая прозрачность теперь работает корректно для всех типов медиа
- **Приоритет общих настроек**: Общая прозрачность имеет приоритет над личными настройками элементов
- **Синхронизация между экранами**: Прозрачность корректно применяется на основном и дополнительном экранах
- **Интуитивная работа**: При включении/выключении общей прозрачности изменения применяются мгновенно
- **Сохранение настроек**: Общая прозрачность автоматически сохраняется в проект

### [2024-12-19] - Исправление бага дублирования текста при ручной настройке положения

**Исправленная проблема:**
- **Баг дублирования текста**: ОКОНЧАТЕЛЬНО исправлена проблема, когда при включении ручной настройки положения текста создавался дублирующий текстовый блок. Теперь при изменении настроек текста (включение/выключение ручной настройки положения, изменение координат X/Y) текст не дублируется.

**Техническая реализация:**
- **Улучшена логика в `LoadMediaFromSlotSelective`**: Добавлена проверка существования текстового элемента в `textOverlayGrid` перед созданием нового. Если текстовый элемент уже существует, обновляются его свойства вместо создания нового.
- **Исправлена синхронизация с дополнительным экраном**: Аналогичная логика применена для синхронизации текста на втором экране - проверяется существование текстового элемента перед созданием нового.
- **Предотвращение дублирования**: Теперь при любых изменениях настроек текста (шрифт, размер, цвет, позиция, ручная настройка координат) используется существующий текстовый элемент вместо создания нового.

**Пользовательский опыт:**
- **ПРОБЛЕМА РЕШЕНА**: При включении ручной настройки положения текста больше НЕ создается дублирующий текст
- **Корректная работа настроек**: Все настройки текста (шрифт, размер, цвет, позиция, координаты X/Y) работают без создания дублирующих элементов
- **Стабильная синхронизация**: Текст корректно синхронизируется между основным и дополнительным экранами без дублирования
- **Улучшенная производительность**: Устранены лишние создания UI элементов, что улучшает производительность приложения

### [2024-12-19] - ОКОНЧАТЕЛЬНЫЕ исправления текста и медиа

**Исправленные проблемы:**
- **ИСПРАВЛЕНО воспроизведение медиа после текста**: Полностью переработана логика работы с `textOverlayGrid`. Проблема была в том, что при замене содержимого `mediaBorder` терялся `textOverlayGrid`, который находится внутри Grid как дочерний элемент.
- **ИСПРАВЛЕН серый цвет медиа**: Исправлена проблема, когда медиа отображалось серым цветом. Проблема была в том, что `textOverlayGrid` всегда присутствовал в Grid и перекрывал медиа, даже когда в нем не было текста. Теперь `textOverlayGrid` скрывается когда в нем нет текста.
- **ИСПРАВЛЕНО применение настроек текста**: Исправлена ошибка в методе `ApplyTextSettings`, где для вторичного экрана использовалась неправильная прозрачность (`_selectedElementSlot.Opacity` вместо `1.0`). Также исправлена логика управления видимостью `textOverlayGrid`.
- **ИСПРАВЛЕНО исчезновение текста**: КАРДИНАЛЬНО переработан метод `UpdateMediaElement` и все методы загрузки медиа. Теперь вместо замены содержимого `mediaBorder` мы работаем с существующим Grid и заменяем только медиа элементы, сохраняя `textOverlayGrid`.
- **Добавлена поддержка текстовых элементов в панели настроек**: Добавлена полная поддержка текстовых элементов в методе `ElementPlay_Click` с корректной логикой определения состояния воспроизведения.

**Техническая реализация:**
- **КАРДИНАЛЬНО переработан `UpdateMediaElement`**: Теперь метод работает с существующим Grid внутри `mediaBorder`, удаляет только старые медиа элементы и добавляет новые, сохраняя `textOverlayGrid`
- **Добавлено управление видимостью `textOverlayGrid`**: Теперь `textOverlayGrid` автоматически скрывается когда в нем нет текста и показывается когда есть текст
- **Исправлена логика создания текстовых элементов**: Текстовые элементы теперь добавляются в `textOverlayGrid`, а не в основной Grid
- **Исправлена логика для изображений**: В `LoadMediaFromSlotSelective` и `ElementPlay_Click` для изображений теперь используется та же логика сохранения `textOverlayGrid`
- **Исправлена логика для видео**: В `ElementPlay_Click` для видео добавлена проверка существования Grid и сохранение `textOverlayGrid`
- **Улучшен метод `ApplyTextSettings`**: Добавлено управление видимостью `textOverlayGrid` в зависимости от настроек текста
- **Добавлена отладочная информация**: Добавлены отладочные сообщения в ключевые места для диагностики проблем
- **Исправлена прозрачность текста на вторичном экране**: Теперь всегда 100% прозрачность
- **Улучшена логика определения состояния воспроизведения для текстовых элементов**

**Пользовательский опыт:**
- **Медиа теперь КОРРЕКТНО воспроизводится после отображения текстовых блоков**
- **Медиа больше НЕ отображается серым цветом - проблема полностью решена**
- **Настройки текста теперь РАБОТАЮТ корректно на основном и дополнительном экранах**
- **Текстовые блоки НЕ исчезают при переключении между медиа**
- **Панель настроек корректно работает с текстовыми элементами**
- **Текст остается видимым при переключении между различными типами медиа**
- **textOverlayGrid автоматически скрывается когда нет текста и показывается когда есть**
- **Добавлена отладочная информация в консоль для диагностики проблем**

### [2024-12-19] - Исправления багов с текстовыми блоками и расширенные настройки

**Исправленные проблемы:**
- **Исправлены кнопки в диалоге ввода текста**: Убраны разноцветные линии, применены нормальные стили кнопок с правильными цветами (зеленая OK, красная Отмена)
- **Исправлена проблема с зеленым цветом кнопки картинки**: При переключении с картинки на текст кнопка картинки теперь сохраняет зеленый цвет благодаря вызову `UpdateAllSlotButtonsHighlighting()` при создании текстового блока
- **Добавлена кнопка "убрать" для текста**: В настройках текстовых блоков добавлена кнопка для скрытия/показа текста на медиаплеере, при этом текст остается на сцене
- **Исправлен вылет приложения при создании текста**: Добавлены проверки на null и обработка исключений в методе `CreateTextBlock()`
- **Исправлены предупреждения компилятора**: Устранены все критические предупреждения о nullable ссылках и скрытии наследуемых членов
- **Исправлена ошибка Template в кнопках**: Убран проблемный `Template="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"` из кнопок диалога ввода текста
- **Исправлено закрепление текста**: Текстовые блоки теперь остаются на экране при переключении медиа, не скрываются автоматически
- **Обновлены настройки текста**: Убрана отдельная прозрачность текста, добавлен выбор шрифта из 8 популярных вариантов
- **Исправлено воспроизведение медиа**: Медиа теперь корректно воспроизводится после текстовых блоков
- **Исправлено применение шрифтов**: Шрифты теперь корректно применяются к текстовым блокам
- **Убраны лишние настройки**: При выборе текстового элемента скрываются ненужные настройки (скорость, прозрачность, звук)
- **Исправлена работа медиа**: Медиа теперь работает стабильно с новой структурой медиаплеера

**Новые функции:**
- **Расширенные настройки текста**: Добавлена полная панель настроек для текстовых блоков с возможностью настройки:
  - **Цвет текста**: Выбор из 8 предустановленных цветов (белый, черный, красный, зеленый, синий, желтый, оранжевый, фиолетовый)
  - **Шрифт**: Выбор из 8 популярных шрифтов (Arial, Times New Roman, Calibri, Verdana, Georgia, Comic Sans MS, Impact, Trebuchet MS)
  - **Размер шрифта**: Слайдер от 12px до 72px с отображением текущего значения
  - **Положение текста**: 9 предустановленных позиций (TopLeft, TopCenter, TopRight, CenterLeft, Center, CenterRight, BottomLeft, BottomCenter, BottomRight)
  - **Ручная настройка положения**: Возможность задать точные координаты X и Y в пикселях
  - **Управление видимостью**: Кнопка для скрытия/показа текста на медиаплеере без удаления со сцены
  - **Закрепление текста**: Текстовые блоки остаются на экране при переключении между видео, изображениями и другими медиа

**Техническая реализация:**
- Добавлено поле `IsTextVisible` в модель `MediaSlot` для управления видимостью текста
- Создана новая секция `TextSettingsGroupBox` в панели настроек элемента
- Реализованы обработчики событий для всех новых элементов управления текстом
- Добавлен метод `ApplyTextSettings()` для применения настроек к отображаемому тексту
- Добавлен метод `LoadTextSettings()` для загрузки настроек текста в UI
- Настройки текста применяются синхронно на основном и дополнительном экранах
- Все настройки сохраняются в JSON файле проекта
- Исправлено имя TextBox в `TextInputDialog.xaml` (TextInput → TextInputBox) для устранения конфликта с наследуемым членом
- Добавлены проверки на null и обработка исключений в критических методах
- Исправлены все предупреждения компилятора о nullable ссылках
- Убран проблемный Template из кнопок диалога ввода текста для устранения исключения при создании текстовых блоков
- Изменена структура медиаплеера: добавлен `textOverlayGrid` для постоянного отображения текстовых блоков поверх медиа
- Создан метод `UpdateMediaElement()` для обновления медиа без потери текстовых блоков
- Добавлен выбор шрифта с 8 предустановленными вариантами и соответствующий обработчик `FontFamilyComboBox_SelectionChanged`
- Убрана отдельная настройка прозрачности текста - теперь всегда 100% прозрачность
- Создан метод `RestoreMediaElement()` для безопасного восстановления MediaElement в Border
- Добавлены имена для GroupBox элементов (SpeedGroupBox, OpacityGroupBox, VolumeGroupBox) для управления видимостью
- Обновлен метод `LoadElementSettings()` для скрытия ненужных настроек при выборе текстовых элементов
- Исправлена логика обновления MediaElement в существующем Grid для сохранения текстовых блоков

**Пользовательский опыт:**
- Интуитивные кнопки в диалоге ввода текста без визуальных артефактов
- Стабильная подсветка кнопок слотов при переключении между типами контента
- Полный контроль над внешним видом текстовых блоков
- Текстовые блоки остаются на экране при переключении медиа - больше не нужно заново добавлять текст
- Упрощенные настройки текста с фокусом на основные параметры (шрифт, размер, цвет, позиция)
- Возможность выбора из популярных шрифтов для лучшего оформления текста
- Возможность скрыть текст с медиаплеера, оставив его на сцене для последующего показа
- Синхронизация всех настроек между основным и дополнительным экранами
- Чистый интерфейс настроек: при выборе текстового элемента показываются только релевантные настройки
- Стабильная работа медиа: воспроизведение работает корректно независимо от наличия текстовых блоков
- Корректное применение всех настроок текста в реальном времени

### [2024-12-19] - Окончательные исправления текстовых блоков

**Исправленные проблемы:**
- **Исправлены стили кнопок в диалоге ввода текста**: Применены простые но эффективные стили (зеленая OK, красная Отмена) с жирным шрифтом
- **Исправлена ошибка WPF**: Решена проблема "элемент уже является логическим дочерним" при добавлении текста поверх медиа
- **Улучшена логика управления UI элементами**: Правильное отсоединение элементов от родительских контейнеров перед переносом
- **Стабильная работа параллельного воспроизведения**: Текстовые блоки корректно отображаются поверх видео/фото без ошибок
- **Добавлена установка _currentMainMedia**: Текстовые блоки теперь правильно устанавливаются как активное медиа

**Новые функции:**
- **9 предустановленных позиций текста**: TopLeft, TopCenter, TopRight, CenterLeft, Center, CenterRight, BottomLeft, BottomCenter, BottomRight
- **Ручная настройка положения**: Возможность задать точные координаты X и Y для позиционирования текста
- **Параллельное воспроизведение**: Текстовые блоки работают как аудио - не заменяют визуальный контент, а добавляются поверх него
- **Синхронизация с вторым экраном**: Все настройки текста применяются синхронно на основном и дополнительном экранах

**Техническая реализация:**
- Добавлены поля в модель `MediaSlot`: TextPosition, TextX, TextY, UseManualPosition
- Созданы вспомогательные методы: GetTextAlignment, GetVerticalAlignment, GetHorizontalAlignment
- Обновлена логика в `LoadMediaFromSlotSelective` для параллельного отображения текста
- **Исправлены стили кнопок**: Применены простые стили Background/Foreground для корректного отображения
- **Исправлена ошибка WPF**: Правильное отсоединение UI элементов от родительских контейнеров
- Добавлена поддержка Grid с Z-Index для правильного наложения элементов
- Улучшена обработка как основного, так и дополнительного экранов
- Добавлена установка `_currentMainMedia = slotKey` для текстовых блоков

### [2024-12-19] - Добавление поддержки текстовых блоков

**Новые функции:**
- **Текстовые блоки**: Добавлена возможность создания текстовых блоков в слотах
- **Диалог выбора типа контента**: При клике на пустой слот появляется диалог для выбора между загрузкой медиафайла и созданием текстового блока
- **Отображение текста в медиаплеере**: Текстовые блоки отображаются в медиаплеере с настраиваемыми параметрами (шрифт, размер, цвет)
- **Синхронизация с вторым экраном**: Текстовые блоки поддерживают вывод на второй экран с полной синхронизацией
- **Сохранение в проекте**: Текстовые блоки сохраняются в JSON файле проекта со всеми настройками

**Техническая реализация:**
- Добавлен новый тип медиа `MediaType.Text` в enum
- Расширена модель `MediaSlot` полями для текстовых настроек (TextContent, FontFamily, FontSize, FontColor, BackgroundColor)
- Создан диалог `TextInputDialog` для ввода текста
- Обновлен метод `LoadMediaFromSlotSelective` для поддержки отображения текстовых блоков
- Добавлена поддержка текстовых блоков в `ProjectManager` и `UpdateSlotButton`
- Обновлены методы `GetMediaIcon` и `GetMediaTypeName` для текстовых блоков

**Пользовательский опыт:**
- Интуитивный диалог выбора типа контента при создании нового слота
- Простой ввод текста с поддержкой многострочного ввода
- Красивое отображение текста в медиаплеере с центрированием и переносом строк
- Полная интеграция с существующей системой слотов и проектов
- Поддержка всех переходов и анимаций для текстовых блоков

### [2024-12-19] - Исправление проблем с синхронизацией второго экрана и автоматическим скипом видео

**Исправленные проблемы:**
- **Синхронизация между экранами при переходе от картинки к видео**: Исправлена проблема, когда при переходе от изображения к видео на основном экране показывалось видео, а на втором экране оставалась картинка. Теперь оба экрана синхронизированы корректно.
- **Автоматический скип видео при малом остатке времени**: Исправлена проблема, когда видео автоматически скипалось если оставалось меньше 0.4 секунды до конца. Теперь если при восстановлении позиции остается меньше 0.4 секунды, видео запускается с самого начала.

**Техническая реализация:**
- Исправлена синхронизация второго экрана: при переходе к видео восстанавливается `MediaElement` как `Content` окна второго экрана
- Добавлена проверка остатка времени видео: если `remainingTime < 0.4 секунды`, то `Position = TimeSpan.Zero`
- Синхронизация позиции между основным и вторым экраном при автоматическом перезапуске
- Улучшена логика восстановления позиции видео с учетом длительности медиафайла

**Пользовательский опыт:**
- Корректная синхронизация между основным и вторым экраном при смене типов медиа
- Видео не скипается автоматически при малом остатке времени - запускается с начала
- Плавные переходы между изображениями и видео на обоих экранах

### [2024-12-19] - Исправление проблемы с переоткрытием окна второго экрана

**Исправленная проблема:**
- **Переоткрытие окна второго экрана**: Исправлена проблема, когда окно вывода медиа на второй экран переоткрывалось при каждом запуске медиафайла. Теперь окно создается только один раз при включении функции вывода на второй экран и остается открытым до отключения функции.

**Техническая реализация:**
- Убран вызов `CreateSecondaryScreenWindow()` из метода `LoadMediaFromSlotSelective()` - окно больше не пересоздается при каждой загрузке медиа
- Добавлен вызов `CreateSecondaryScreenWindow()` сразу после выбора экрана в диалоге настройки устройств
- Добавлен вызов `CloseSecondaryScreenWindow()` при отключении функции вывода на второй экран
- Окно второго экрана теперь создается только при включении функции и закрывается только при отключении

**Пользовательский опыт:**
- Окно второго экрана больше не мигает и не переоткрывается при смене медиа
- Плавная работа с выводом на второй экран без визуальных артефактов
- Стабильное поведение окна второго экрана при воспроизведении медиа

### [2024-12-19] - Исправление проблем с выводом на второй экран

**Исправленные проблемы:**
- **MessageBox при включении медиа на втором экране**: Убран назойливый MessageBox, который появлялся при создании окна на втором экране. Теперь информация выводится только в отладочную консоль
- **Переходы на втором экране**: Исправлена проблема с переходами между медиа на втором экране. Теперь все типы переходов (затухание, скольжение, увеличение/уменьшение) работают синхронно на обоих экранах
- **Синхронизация настроек с вторым экраном**: Исправлена проблема, когда настройки скорости воспроизведения (например, x2) применялись только к основному экрану, а на втором экране медиа воспроизводилось с обычной скоростью
- **Дублирование звука при выводе на второй экран**: Исправлена проблема дублирования звука, когда звук воспроизводился одновременно на основном и втором экране
- **Баг с анимацией затухания**: Исправлена проблема, когда при анимации затухания второй экран полностью исчезал на несколько миллисекунд, а затем снова появлялся
- **Синхронизация анимаций переходов**: Исправлена проблема с анимациями переходов - теперь все анимации (затухание, скольжение, зум) работают синхронно на обоих экранах
- **Мигание второго экрана**: Исправлена проблема с миганием второго экрана при смене контента во время анимации - теперь контент меняется незаметно до начала анимации
- **Спойлер медиа**: Исправлена проблема, когда новое медиа показывалось на втором экране до начала анимации затухания - теперь контент меняется только когда экраны невидимы

**Техническая реализация:**
- Заменен MessageBox на System.Diagnostics.Debug.WriteLine в методе `CreateSecondaryScreenWindow()`
- Создан новый метод `ApplyTransitionWithSecondaryScreen()` для применения переходов с поддержкой второго экрана
- Добавлены методы переходов с поддержкой второго экрана:
  - `ApplyFadeTransitionWithSecondaryScreen()` - переход затуханием
  - `ApplySlideTransitionWithSecondaryScreen()` - переход скольжением  
  - `ApplyZoomTransitionWithSecondaryScreen()` - переход увеличением/уменьшением
- Обновлены вызовы переходов в методах загрузки видео и изображений для использования новых методов
- Переходы теперь применяются синхронно к основному и дополнительному экранам
- Добавлена синхронизация настроек скорости воспроизведения (`SpeedRatio`) между основным и вторым экраном в методах `ApplyElementSettings()`
- Отключен звук на втором экране (`Volume = 0`) при создании `_secondaryMediaElement` и в методах применения настроек
- Синхронизирована прозрачность изображений и видео между экранами
- Исправлена логика анимаций переходов: анимация теперь применяется синхронно к обоим экранам одновременно
- Восстановлена синхронизация всех типов переходов: затухание, скольжение и зум работают на обоих экранах одновременно
- Исправлена проблема с миганием экрана - теперь анимации работают плавно без исчезновения контента
- Изменен порядок выполнения переходов: контент на втором экране меняется до начала анимации, что исключает мигание
- Оптимизирована синхронизация: переход на втором экране выполняется незаметно, пока экран полностью видимый
- Исправлен спойлер медиа: контент на обоих экранах меняется только когда экраны невидимы (в момент минимальной прозрачности/максимального сдвига/минимального размера)
- Улучшена логика переходов: сначала анимация скрытия, затем смена контента, затем анимация показа

**Пользовательский опыт:**
- При создании окна на втором экране больше не появляются назойливые MessageBox
- Переходы между медиа теперь работают плавно и синхронно на обоих экранах
- Настройки скорости воспроизведения (x0.5, x2, x3 и т.д.) теперь применяются синхронно на обоих экранах
- Звук воспроизводится только на основном экране, исключая дублирование
- Анимации переходов теперь работают синхронно на обоих экранах
- Все типы переходов (затухание, скольжение, зум) работают плавно и красиво на обоих экранах
- Второй экран синхронизирован с основным экраном во время всех анимаций
- Устранено мигание второго экрана - переходы теперь выглядят профессионально без визуальных артефактов
- Контент на втором экране меняется незаметно, обеспечивая плавность анимаций
- Исключен спойлер медиа - новое медиа не показывается до завершения анимации скрытия
- Переходы работают как в профессиональных медиа-приложениях - сначала скрытие, затем смена контента, затем показ
- Улучшена стабильность работы с дополнительными экранами
- Отладочная информация доступна в консоли для диагностики

### [2024-12-19] - Реализация перетаскиваемых окон настроек с маяками

**Новые функции:**
- **Перетаскиваемые окна настроек**: Панели настроек элемента и общих настроек проекта теперь открываются в отдельных перетаскиваемых окнах
- **Маяки для изменения размера**: Каждое окно настроек имеет маяк внизу для изменения высоты окна
- **Кнопка закрытия**: Каждое окно имеет кнопку "✕" в заголовке для закрытия
- **Перетаскивание окон**: Окна можно перетаскивать за заголовок и располагать поверх друг друга
- **Кнопки открытия окон**: Добавлены кнопки "Настройки элемента" и "Общие настройки" в главном окне

**Исправленные проблемы:**
- **Закрытие окна вывода на второй монитор**: При закрытии приложения теперь корректно закрывается и окно вывода медиа на второй монитор
- **Логика кнопок навигации**: Кнопки "◀" и "▶" в настройках элемента теперь переключают медиа (следующее/предыдущее) и запускают его воспроизведение, а не переключают окно настроек

**Техническая реализация:**
- Созданы новые окна `ElementSettingsWindow.xaml` и `GlobalSettingsWindow.xaml` с прозрачным фоном и скругленными углами
- Реализованы обработчики для перетаскивания окон (`Window_MouseLeftButtonDown`, `Window_MouseMove`, `Window_MouseLeftButtonUp`)
- Добавлены маяки для изменения размера (`ResizeHandle_MouseLeftButtonDown`)
- Создан новый метод `NavigateToMediaAndPlay()` для переключения медиа с запуском воспроизведения
- Добавлен обработчик `Window_Closing` для корректного закрытия всех окон при закрытии приложения
- Старые панели настроек в главном окне скрыты (`Visibility="Collapsed"`)

**Пользовательский опыт:**
- Окна настроек можно свободно перемещать и располагать поверх друг друга
- Каждое окно можно растягивать по высоте с помощью маяка внизу
- Кнопки навигации в настройках элемента теперь интуитивно переключают медиа
- При закрытии приложения все окна корректно закрываются
- Улучшена организация рабочего пространства

### [2024-12-19] - Исправление проблем с отображением изображений и кнопкой удаления

**Исправленные проблемы:**
- **Обрезание изображений**: Исправлена проблема с обрезанием изображений в основном плеере и на дополнительном экране. Изменен `Stretch.UniformToFill` на `Stretch.Uniform` для корректного отображения без обрезания
- **Кнопка удаления медиа**: Добавлена отладочная информация в метод `DeleteItem_Click` для диагностики проблем с удалением медиа из слотов
- **Размер медиа в плеере**: Исправлено отображение изображений в мини-проигрывателе - теперь изображения корректно масштабируются без обрезания

**Техническая реализация:**
- Изменен `Stretch.UniformToFill` на `Stretch.Uniform` во всех местах создания изображений:
  - В основном плеере при загрузке изображений
  - На дополнительном экране при синхронизации
  - В триггерах при воспроизведении изображений
- Добавлены отладочные сообщения в `DeleteItem_Click` для отслеживания процесса удаления
- Улучшена диагностика: добавлены сообщения о попытке удаления, поиске слота и результате операции

**Пользовательский опыт:**
- Изображения теперь отображаются полностью без обрезания краев
- Правильное масштабирование изображений в плеере с сохранением пропорций
- Отладочная информация помогает диагностировать проблемы с удалением медиа
- Улучшенное отображение на дополнительном экране

### [2024-12-19] - Исправление бага с выводом на мониторы и обновление .NET до версии 8.0

**Исправленные проблемы:**
- **Баг с выводом на неправильный монитор**: Исправлена проблема, когда при выборе второго монитора медиа все равно выводилось на главный экран. Проблема была в использовании `WindowState.Maximized`, которое игнорировало координаты окна. Теперь используется `WindowState.Normal` с точными координатами экрана
- **Обновление .NET**: Проект успешно обновлен с .NET 7.0 на .NET 8.0 для получения улучшений производительности и новых возможностей

**Техническая реализация:**
- Изменен `WindowState.Maximized` на `WindowState.Normal` в методе `CreateSecondaryScreenWindow()`
- Добавлена отладочная информация для отслеживания создания окна и синхронизации медиа
- Добавлены MessageBox уведомления для подтверждения выбора экрана и создания окна
- Улучшена диагностика: добавлены отладочные сообщения для синхронизации видео, изображений и воспроизведения
- Исправлена ошибка компиляции с переменной `screenIndex` в MessageBox

**Пользовательский опыт:**
- При выборе экрана теперь показывается подтверждение с информацией о разрешении и позиции
- При создании окна на дополнительном экране показывается уведомление с деталями
- Отладочная информация в консоли помогает диагностировать проблемы с выводом на мониторы
- Проект теперь использует .NET 8.0 с улучшенной производительностью

### [2024-12-19] - Расширенная навигация и реальный вывод на устройства

**Новые функции:**
- **Кнопки навигации в панели настроек элемента**: Добавлены кнопки "◀" (назад) и "▶" (вперед) в панель настроек элемента для быстрого переключения между элементами проекта
- **Кнопки переключения медиа**: Добавлены кнопки "⏮️" (предыдущее медиа) и "⏭️" (следующее медиа) в управление воспроизведением для переключения между медиафайлами
- **Меню Edit с подменю устройств**: Добавлено новое меню "Edit" рядом с "File" с двумя подменю:
  - **Экраны**: Отображает все подключенные к компьютеру мониторы с их разрешением и позицией
  - **Звук**: Показывает все доступные устройства вывода звука
- **Обновление устройств в реальном времени**: При клике на меню Edit автоматически обновляется список подключенных устройств
- **Диалоги выбора устройств**: При клике на экран или аудиоустройство открывается диалог с настройками:
  - Название устройства и техническая информация
  - Чекбокс "Воспроизводить на этом устройстве"
  - Защита от выбора основного экрана для вывода
- **Реальный вывод на устройства**: 
  - Выбранный экран: Создается полноэкранное окно на дополнительном мониторе с синхронизированным воспроизведением
  - Выбранное аудиоустройство: Настраивается вывод звука на конкретное устройство
  - Синхронизация: Видео и изображения воспроизводятся одновременно на основном и дополнительном экранах

**Техническая реализация:**
- Добавлены методы `NavigateToElement()`, `PreviousElementButton_Click()`, `NextElementButton_Click()` для навигации между элементами
- Реализованы методы `InitializeScreensMenu()` и `InitializeAudioMenu()` для динамического заполнения меню устройств
- Добавлен обработчик `EditMenuItem_SubmenuOpened()` для обновления устройств в реальном времени
- Реализованы диалоги `ShowScreenSelectionDialog()` и `ShowAudioSelectionDialog()` для настройки устройств
- Методы `CreateSecondaryScreenWindow()` и `CloseSecondaryScreenWindow()` для управления дополнительными экранами
- Синхронизация воспроизведения между основным и дополнительным экранами
- Использование NAudio для получения реальных аудиоустройств
- Использование System.Windows.Forms.Screen для получения информации о мониторах

**Пользовательский опыт:**
- Удобная навигация между элементами проекта без необходимости кликать по слотам
- Быстрый доступ к информации о подключенных мониторах и аудиоустройствах
- Интуитивно понятные иконки навигации (◀ и ▶)
- Автоматическое скрытие кнопок навигации при отсутствии элементов
- Реальный вывод медиа на выбранные устройства
- Защита от случайного выбора основного экрана
- Синхронизированное воспроизведение на нескольких экранах

### [2024-12-19] - Исправление багов с запоминанием позиций, параллельным воспроизведением аудио и автопереходом

**Исправленные проблемы:**
- **Баг с запоминанием позиции видео**: Исправлена проблема, когда видео не запоминало свою позицию при переключении между слотами и начиналось с 0. Теперь позиция корректно сохраняется и восстанавливается при возврате к предыдущему слоту
- **Баг с параллельным воспроизведением аудио**: Исправлена проблема, когда при запуске нового аудио предыдущее не останавливалось, что приводило к одновременному воспроизведению нескольких аудио файлов. Теперь предыдущее аудио корректно останавливается при запуске нового
- **Добавлен автопереход для аудио**: Теперь аудио файлы автоматически переходят к следующему элементу в той же строке при окончании воспроизведения, как это работает для видео

**Техническая реализация:**
- Раскомментирован метод `StopActiveAudio()` для корректной остановки предыдущего аудио
- Добавлен вызов `SaveSlotPosition()` в `LoadMediaFromSlotSelective()` для сохранения позиции текущего слота
- Восстановлены вызовы `StopActiveAudio()` в методах управления аудио
- Упрощена логика восстановления позиции для видео - теперь используется та же простая логика, что и для аудио (`GetSlotPosition()`)
- Добавлен метод `AutoPlayNextAudioElement()` для автоперехода аудио элементов
- Добавлен обработчик события `MediaEnded` для аудио элементов с вызовом автоперехода

**Пользовательский опыт:**
- Видео теперь корректно запоминает позицию при переключении между слотами (исправлено!)
- При запуске нового аудио предыдущее автоматически останавливается
- Аудио файлы теперь автоматически переходят к следующему элементу при окончании воспроизведения
- Улучшена стабильность работы с медиафайлами
- Исправлено поведение, которое работало корректно ранее

### [2024-12-19] - Добавление общих настроек проекта и переходов
- **Реализованы общие настройки проекта**: Добавлена панель общих настроек рядом с личными настройками элементов
- **Приоритет общих настроек**: Общие настройки громкости и прозрачности имеют приоритет над личными настройками элементов
- **Настройки переходов**: Добавлена возможность настройки типа и длительности переходов между медиа элементами:
  - Мгновенный переход (по умолчанию)
  - Плавное затухание (fade)
  - Скольжение (slide)
  - Увеличение/уменьшение (zoom)
- **Автопереход на следующий элемент**: Реализована функция автоматического воспроизведения следующего элемента в той же строке
- **Дополнительные настройки**: Добавлены опции автоматического воспроизведения следующего элемента и зацикливания плейлиста
- **Автоматическое сохранение**: Общие настройки автоматически сохраняются в JSON файл проекта при каждом изменении
- **Сохранение позиций медиа**: Исправлена проблема с сохранением позиции воспроизведения при переключении между слотами
- **Восстановление позиций**: Добавлено корректное восстановление позиции воспроизведения при возврате к предыдущему слоту
- **Гибкость управления**: Личные настройки элементов сохраняются и не изменяются - они просто игнорируются при включенных общих настройках

### [2024-12-19] - Финальные исправления панели настроек элементов
- **Исправлена проблема с прогрузкой при скорости меньше 1**: Добавлена проверка `!SpeedSlider.IsMouseCaptured` в `SpeedSlider_ValueChanged` - настройки применяются только после окончания перетаскивания слайдера, что предотвращает частые перезагрузки медиа
- **Исправлена проблема с паузой звука при зажатии слайдера**: Добавлен обработчик `SpeedSlider_PreviewMouseLeftButtonUp` для применения настроек только после отпускания слайдера
- **Исправлена подсветка кнопок элементов**: Добавлены вызовы `UpdateAllSlotButtonsHighlighting()` во все методы управления воспроизведением в панели настроек, теперь кнопки слотов корректно подсвечиваются зеленым при воспроизведении
- **Улучшена отзывчивость интерфейса**: Слайдеры теперь работают плавно без прерывания воспроизведения во время перетаскивания

### [2024-12-19] - Дополнительные исправления панели настроек элементов
- **Исправлена логика пауза/плей с сохранением позиции**: Кнопка ▶️ теперь использует ту же логику, что и кнопки внизу - сохраняет позицию при паузе и восстанавливает при возобновлении через `_mediaResumePositions`
- **Добавлен пункт "Настройки" в контекстное меню**: Теперь можно открыть настройки элемента через правый клик по слоту, что предотвращает случайное переключение настроек при запуске других элементов
- **Исправлено применение прозрачности к изображениям**: Прозрачность теперь корректно применяется к Border контейнеру для изображений (`mediaBorder.Opacity`) и к MediaElement для видео
- **Улучшено управление состоянием кнопок**: Кнопки "Остановить" и "Заново" теперь правильно сбрасывают состояние кнопки "Продолжить"

### [2024-12-19] - Исправление багов панели настроек элементов
- **Исправлена логика кнопки "Продолжить"**: Кнопка ▶️ теперь корректно работает как пауза/возобновление. Если медиа уже играет - ставит на паузу, если на паузе - возобновляет, если не играет - запускает заново
- **Исправлена логика кнопки "Заново"**: Кнопка ⏮️ теперь правильно перезапускает медиа с самого начала и сбрасывает состояние кнопки "Продолжить"
- **Исправлено автоматическое применение настроек**: Настройки элементов (скорость, прозрачность, громкость) теперь автоматически применяются к активным медиа элементам при загрузке проекта, без необходимости "дергать" слайдеры
- **Улучшена визуальная обратная связь**: Кнопка "Продолжить" динамически меняет иконку (▶️/⏸️) и подсказку в зависимости от состояния воспроизведения

### [2024-12-19] - Восстановление меню File и обновление документации
- **Восстановлено меню File**: Раскомментировано меню File в MainWindow.xaml, которое было отключено. Теперь доступны функции New Project, Open Project, Save Project и Exit
- **Исправлены ошибки компиляции**: Перенесены методы-обработчики меню File (NewProject_Click, OpenProject_Click, SaveProject_Click, Exit_Click) во второй partial класс MainWindow для корректной компиляции
- **Обновлена документация**: Перенесена информация о панели настроек элемента из ArenaApp/README_RU.md в основную документацию README_RU.md
- **Добавлен раздел "Панель настроек элемента"**: Описаны все возможности настройки элементов медиа (скорость воспроизведения, прозрачность, уровень звука, переименование)
- **Удален дублирующий файл**: Удален ArenaApp/README_RU.md, так как вся информация перенесена в основной файл документации

### [2024-12-19] - ИСПРАВЛЕНИЕ дублирования аудио при запуске триггера колонки

**Исправленная проблема:**
- **Дублирование аудио при запуске триггера**: ОКОНЧАТЕЛЬНО исправлена проблема, когда при запуске аудио, а затем запуске триггера этой же колонки, создавался дублирующийся звук. Теперь если аудио уже играет и запускается триггер этой колонки, то аудио НЕ ТРОГАЕТСЯ - просто ничего с ним не делается, ни с 0, ни новое. Аудио продолжает играть с той же позиции, как будто ничего не изменилось.

**Техническая реализация:**
- **Расширена логика проверки аудио**: В `StartVideoWithAudio` и `StartImageWithAudio` добавлена проверка `wasAudioPlayingInTrigger` для определения, играет ли аудио уже в триггере (включая тот же триггер)
- **Улучшена логика переиспользования**: Теперь аудио не трогается, если оно уже играет в триггере, а не только в отдельном слоте
- **Обновлена логика в `SmartStopTriggers`**: Добавлена поддержка переиспользования аудио из триггеров (`_currentAudioContent.StartsWith("Trigger_")`)
- **Исправлена логика создания элементов**: Аудио элемент не создается заново, если уже играет в триггере
- **Исправлена логика запуска**: Аудио не запускается заново, если уже играет в триггере

**Пользовательский опыт:**
- **ПРОБЛЕМА РЕШЕНА**: При запуске аудио, а затем запуске триггера этой же колонки - аудио НЕ ТРОГАЕТСЯ! Просто ничего с ним не делается, ни с 0, ни новое. Аудио продолжает играть с той же позиции, как будто ничего не изменилось
- Нет дублирующегося звука при повторном запуске триггера с тем же аудио
- Аудио корректно продолжает воспроизведение без перерыва
- Визуальная часть (видео/изображение) корректно заменяется при сохранении аудио

### [2024-12-19] - ОКОНЧАТЕЛЬНОЕ исправление логики аудио в триггерах

**Исправленные проблемы:**
- **Двойное воспроизведение аудио**: ОКОНЧАТЕЛЬНО исправлена проблема двойного воспроизведения - теперь если аудио уже играет в слоте и запускается триггер с этим же аудио, то НЕ ЗАПУСКАЕТСЯ новое и НЕ ПЕРЕМАТЫВАЕТСЯ старое. Аудио просто продолжает играть с той же позиции, как будто ничего не изменилось
- **Перезапуск аудио с нуля**: КАРДИНАЛЬНО исправлена проблема, когда при запуске триггера с уже играющим аудио, звук перезапускался с нуля. Теперь аудио действительно продолжает играть с той же позиции, где было остановлено
- **Неправильная остановка аудио**: Исправлена логика в `SmartStopTriggers` - теперь аудио не останавливается, если оно будет переиспользовано в новом триггере
- **Проблема с таймером видео**: Исправлена проблема, когда при запуске триггера с изображением+аудио, аудио отображалось в таймере "Видео". Теперь MediaElement правильно очищается при показе изображений
- **Ошибки компиляции**: Исправлены ошибки CS0103 с неопределенной переменной `audioAlreadyPlayingInSlot` в методах `StartVideoWithAudio` и `StartImageWithAudio`. Добавлены локальные определения переменной для корректной работы логики переиспользования аудио элементов
- **Предупреждения nullable**: Исправлены предупреждения CS8618 для полей `_projectManager` и `_previewGenerator` путем добавления модификатора `null!`
- **Улучшение логики слайдеров**: Убрана задержка в 500мс после отпускания видео слайдера, что делает перемотку более отзывчивой. Удалена ненужная переменная `_lastVideoSliderRelease`

**Техническая реализация:**
- **ОКОНЧАТЕЛЬНО исправлена логика сохранения ссылок**: В `StartVideoWithAudio` и `StartImageWithAudio` добавлено сохранение ссылок на аудио элементы (`existingAudioElement`, `existingAudioContainer`, `existingAudioSlotKey`) ДО вызова `SmartStopTriggers`
- **ОКОНЧАТЕЛЬНО упрощена логика в `SmartStopTriggers`**: Переименована переменная в `audioWillBeReused` - аудио НЕ ТРОГАЕТСЯ, если оно будет переиспользовано в новом триггере
- **Исправлена логика проверки аудио**: Переменные `wasAudioPlayingInSlot` проверяются ПЕРЕД вызовом `SmartStopTriggers`, чтобы сохранить состояние до остановки
- **КАРДИНАЛЬНО исправлена логика запуска**: В `StartVideoWithAudio` и `StartImageWithAudio` добавлены проверки `if (!audioAlreadyPlayingInSlot)` - аудио НЕ ЗАПУСКАЕТСЯ, если уже играет
- **Улучшена логика переиспользования**: Используются сохраненные ссылки `existingAudioElement` вместо поиска в `_activeAudioSlots`
- **Исправлена очистка MediaElement**: В `StartImageWithAudio` добавлены вызовы `mediaElement.Stop()` и `mediaElement.Source = null` для правильной очистки при показе изображений
- Исправлены nullable ссылки с помощью оператора `!` для полей, которые гарантированно инициализируются в конструкторе
- Упрощена логика обновления видео слайдера - убрана проверка времени отпускания
- Удалена переменная `_lastVideoSliderRelease` и связанная с ней логика

**Пользовательский опыт:**
- **ОКОНЧАТЕЛЬНО РЕШЕНО**: При запуске триггера с уже играющим аудио - НЕ ЗАПУСКАЕТСЯ новое и НЕ ПЕРЕМАТЫВАЕТСЯ старое! Аудио просто продолжает играть с той же позиции, как будто ничего не изменилось. Если есть картинка или видео в этой колонке, они запускаются, но звук не трогается
- **ПРОБЛЕМА РЕШЕНА**: При запуске триггера с уже играющим аудио, звук действительно продолжает играть с той же позиции, где был - НЕ перезапускается с нуля!
- **ИСПРАВЛЕНА ПРОБЛЕМА С ТАЙМЕРАМИ**: При запуске триггера с изображением+аудио, аудио больше не отображается в таймере "Видео"
- Приложение теперь компилируется без ошибок
- Слайдеры перемотки работают более отзывчиво
- Улучшена стабильность работы с аудио в триггерах
- Аудио корректно переиспользуется между слотами и триггерами

### [2024-12-19] - Исправление проблем с перемоткой видео и параллельным воспроизведением

**Исправленные проблемы:**
- **Проблема с перемоткой видео**: Убрана проверка `_isVideoSliderDragging` из таймера `VideoTimer_Tick`. Теперь слайдер обновляется в реальном времени, и видео реагирует на изменения слайдера мгновенно, без "догоняния" позиции
- **Проблема с аудио в триггерах**: Исправлена логика в `SmartStopTriggers`. Теперь при запуске триггера аудио из отдельного слота не останавливается, если оно будет использоваться в триггере. Это сохраняет позицию воспроизведения и подсветку кнопки
- **Улучшение подсветки кнопок**: Кнопки-триггеры теперь правильно обновляют свое состояние (▶⏹⏸) и подсветку при изменении состояния воспроизведения

**Техническая реализация:**
- Убрана проверка `!_isVideoSliderDragging` из `VideoTimer_Tick` - слайдер теперь обновляется в реальном времени
- Удалены обработчики `VideoSlider_PreviewMouseLeftButtonDown/Up` и переменная `_isVideoSliderDragging` - они больше не нужны
- Исправлена логика в `SmartStopTriggers` - добавлена проверка, не останавливать ли аудио из слота, которое будет использоваться в новом триггере
- Исправлена логика `audioAlreadyPlayingInSlot` в триггерах - теперь проверяется, что аудио играет именно в отдельном слоте (`_currentAudioContent.StartsWith("Slot_")`)
- При запуске триггера с уже играющим аудио из слота используется существующий MediaElement вместо создания нового
- Улучшен метод `UpdateAllSlotButtonsHighlighting()` для правильной обработки кнопок-триггеров
- Добавлены вызовы `UpdateAllSlotButtonsHighlighting()` во все методы управления триггерами

**Пользовательский опыт:**
- Перемотка видео работает в реальном времени без задержек и "догоняния"
- Аудио в триггерах продолжает воспроизведение с правильной позиции (не начинается с 0)
- Кнопка слота с аудио остается зеленой при запуске триггера
- Кнопки-триггеры корректно отображают свое состояние (оранжевый ▶, красный ⏹, желтый ⏸)
- Улучшена общая отзывчивость интерфейса

### [2024-12-19] - Исправление багов с триггерами и улучшение таймеров

**Исправленные проблемы:**
- **Баг с перезапуском аудио в триггерах**: При запуске параллельного воспроизведения (триггера) аудио больше не перезапускается с нуля, если уже играет. Теперь аудио продолжает воспроизведение с текущей позиции
- **Улучшение плавности слайдеров**: Таймеры обновляются каждые 100 миллисекунд вместо 1 секунды, что делает слайдеры перемотки более плавными и отзывчивыми
- **Отображение миллисекунд**: Таймеры теперь показывают время в формате HH:MM:SS.fff (с миллисекундами) для более точного отображения времени
- **Баг с перезапуском аудио в отдельных слотах**: При запуске аудио в отдельном слоте, если это же аудио уже играет в триггере, оно больше не перезапускается с нуля
- **Улучшение перемотки видео**: Исправлена проблема с задержкой при перемотке видео через слайдер
- **Сохранение аудио при смене на видео**: При запуске видео аудио из триггеров больше не останавливается
- **Подсветка активных кнопок**: Кнопки слотов, которые воспроизводятся, теперь подсвечиваются зеленым цветом

**Техническая реализация:**
- Обновлена логика в `StartVideoWithAudio()` и `StartImageWithAudio()` для проверки уже играющего аудио
- Добавлена переменная `audioAlreadyPlaying` для определения, нужно ли создавать новый MediaElement
- Если аудио уже играет, используется существующий MediaElement вместо создания нового
- Изменены интервалы таймеров с `TimeSpan.FromSeconds(1)` на `TimeSpan.FromMilliseconds(100)`
- Обновлен формат отображения времени с `@"hh\:mm\:ss"` на `@"hh\:mm\:ss\.fff"` для показа миллисекунд
- Обновлены начальные значения в XAML с "00:00:00" на "00:00:00.000"
- Исправлены предупреждения компилятора о nullable ссылках
- Добавлена логика `audioAlreadyPlayingInTrigger` в `LoadMediaFromSlotSelective()` для аудио
- Создан метод `StopActiveTriggersVisualOnly()` для остановки только визуальной части триггеров
- Улучшены обработчики слайдеров с `Dispatcher.BeginInvoke()` для более плавной перемотки
- Добавлен метод `UpdateAllSlotButtonsHighlighting()` для подсветки активных кнопок
- Кнопки слотов теперь автоматически подсвечиваются зеленым при воспроизведении

**Пользовательский опыт:**
- При запуске триггера с уже играющим аудио, звук продолжается без перерыва
- Слайдеры перемотки работают плавно и отзывчиво
- Визуальная часть (видео/изображение) корректно заменяется при сохранении аудио
- Улучшена общая плавность интерфейса
- Точное отображение времени с миллисекундами для лучшего контроля воспроизведения
- Аудио не перезапускается при запуске в отдельных слотах, если уже играет в триггере
- Перемотка видео работает без задержек и "догоняния"
- Аудио из триггеров сохраняется при смене на видео
- Активные кнопки слотов подсвечиваются зеленым для лучшей визуальной обратной связи

### [2024-12-19] - Исправление логики паузы/возобновления медиа

**Исправленная проблема:**
- **Проблема с возобновлением после паузы**: Медиа (видео и аудио) теперь корректно возобновляется после паузы. Ранее медиа ставилось на паузу, но не возобновлялось при повторном клике.

**Техническая реализация:**
- Добавлено отслеживание состояния паузы: `_isVideoPaused` для видео и `_audioPausedStates` для аудио
- Заменена ненадежная логика `CanPause` на собственное отслеживание состояния
- Обновлена логика в `Slot_Click()` и `PauseItem_Click()` для правильного переключения пауза/возобновление
- Исправлен метод `GetPauseMenuItemText()` для корректного отображения "Пауза"/"Продолжить"
- Добавлен сброс состояния паузы при запуске нового медиа в `LoadMediaFromSlotSelective()`
- Сброс состояния паузы при перезапуске в `RestartItem_Click()`

**Пользовательский опыт:**
- Пауза/возобновление теперь работает корректно для всех типов медиа
- Контекстное меню правильно показывает "Пауза" или "Продолжить"
- Клик по слоту корректно переключает между паузой и возобновлением
- Функция "заново" сбрасывает состояние паузы

### [2024-12-19] - Исправление проблем с паузой и перезапуском медиа

**Исправленные проблемы:**
- **Проблема с паузой аудио**: Аудио больше не ставится на паузу на секунду и не продолжает играть. Теперь пауза работает корректно
- **Проблема с "заново" для аудио**: Функция "заново" теперь корректно перезапускает аудио с самого начала
- **Проблема с паузой/возобновлением видео**: Исправлены проблемы с управлением видео после добавления слайдеров
- **Убраны отладочные сообщения**: Удалены все MessageBox с отладочной информацией, которые мешали пользователю

**Техническая реализация:**
- Обновлена логика в `Slot_Click()` для корректного определения активного медиа (основное видео/изображение или аудио)
- Исправлена логика в `RestartItem_Click()` для поддержки перезапуска как видео, так и аудио
- Улучшена логика в `PauseItem_Click()` для правильного управления паузой/возобновлением
- Добавлена проверка `isMainMedia` и `isAudioMedia` для раздельного управления разными типами медиа
- Убраны все отладочные MessageBox сообщения из методов `ShouldBlockMediaFile()`, `RestartItem_Click()`, `PauseItem_Click()`

**Пользовательский опыт:**
- Пауза/возобновление теперь работает корректно для всех типов медиа
- Функция "заново" перезапускает медиа с самого начала
- Контекстное меню работает интуитивно без отладочных сообщений
- Слайдеры перемотки не влияют на работу основных функций управления

### [2024-12-19] - Добавление слайдеров перемотки для видео и аудио

**Новая функциональность:**
- **Слайдер видео**: Добавлен под текстом отсчета времени видео, позволяет быстро перематывать видеофайлы
- **Слайдер аудио**: Добавлен под текстом отсчета времени аудио, позволяет быстро перематывать аудиофайлы
- **Интерактивная перемотка**: Перетаскивание ползунка слайдера мгновенно перематывает медиа к нужному моменту
- **Автоматическое обновление**: Слайдеры автоматически обновляются во время воспроизведения, показывая текущий прогресс
- **Умная синхронизация**: Слайдеры не обновляются во время перетаскивания пользователем, предотвращая конфликты

**Техническая реализация:**
- Добавлены переменные `_isVideoSliderDragging`, `_isAudioSliderDragging` для отслеживания состояния перетаскивания
- Переменные `_videoTotalDuration`, `_audioTotalDuration` для хранения общей длительности медиафайлов
- Обработчики событий `PreviewMouseLeftButtonDown/Up` для определения начала и окончания перетаскивания
- Обработчик `ValueChanged` для выполнения перемотки при изменении значения слайдера
- Интеграция с существующими таймерами для синхронизации отображения прогресса

**Пользовательский опыт:**
- Интуитивное управление перемоткой через визуальные слайдеры
- Быстрый доступ к любому моменту видео или аудио
- Синхронизированное отображение прогресса воспроизведения
- Плавная работа без конфликтов между автоматическим обновлением и ручным управлением

### [2024-12-19] - Исправление замены аудио между слотами и триггерами

**Исправленная логика замены аудио:**
- **Проблема решена**: Играет кока в отдельном слоте → запуск триггера2 (тетрадь+морген) → кока останавливается, морген начинает играть
- **Правильная замена**: Аудио из триггеров заменяет аудио из отдельных слотов
- **Сохранение логики**: Аудио из триггеров продолжает играть при переключении между триггерами
- **Интуитивное поведение**: Новое аудио всегда заменяет предыдущее, независимо от источника

**Техническая реализация:**
- Обновлена логика в `StartVideoWithAudio()`, `StartImageWithAudio()`, `StartSingleMedia()`
- Проверка `!_currentAudioContent.StartsWith("Trigger_")` - определение аудио из отдельных слотов
- Вызов `StopActiveAudio()` для остановки аудио из отдельных слотов при запуске триггеров
- Сохранение логики продолжения воспроизведения аудио между триггерами

**Пользовательский опыт:**
- Аудио из триггеров корректно заменяет аудио из отдельных слотов
- Нет параллельного воспроизведения разных аудио файлов
- Интуитивное поведение: новое аудио всегда заменяет предыдущее

### [2024-12-19] - Умная логика триггеров с сохранением аудио

**Интеллектуальная замена для триггеров:**
- **Проблема решена**: Запуск триггера (морген+книга) → попытка запустить триггер2 (тетрадь+морген) → книга заменяется на тетрадь, морген продолжает играть
- **Умная остановка**: При запуске нового триггера аудио продолжает играть, если оно уже воспроизводится
- **Селективная замена**: Визуальный контент (изображения/видео) заменяется, аудио сохраняется
- **Защита от дублирования**: Аудио защищено от дублирования только в отдельных слотах

**Техническая реализация:**
- `SmartStopTriggers()` - умная остановка триггеров без прерывания аудио
- `SmartStopTriggerVisual()` - остановка только визуальной части триггера
- Проверка `_currentAudioContent.StartsWith("Trigger_")` - определение источника аудио
- Логика продолжения воспроизведения аудио при замене визуального контента

**Пользовательский опыт:**
- Триггеры работают интуитивно: аудио продолжает играть, визуал заменяется
- Нет прерывания аудио при переключении между триггерами с общим звуком
- Плавные переходы между комбинациями медиа

### [2024-12-19] - Умная защита от дублирования медиафайлов

**Селективная защита от дублирования:**
- **Аудио файлы**: Защищены от дублирования - если аудио уже воспроизводится, показывается предупреждение
- **Изображения и видео**: Могут заменять друг друга - если файл уже воспроизводится, он заменяется
- Решена проблема: запуск триггера (звук2+картинка1) → попытка запустить триггер2 с картинкой1 → картинка заменяется
- Сохранена защита: запуск триггера (звук1+картинка1) → попытка запустить звук1 отдельно → предупреждение

**Техническая реализация:**
- `HashSet<string> _activeMediaFilePaths` - отслеживание активных медиафайлов по путям
- Метод `ShouldBlockMediaFile()` - селективная проверка (только для аудио)
- Методы `RegisterActiveMediaFile()` и `UnregisterActiveMediaFile()` - управление регистрацией
- Интеграция во все методы воспроизведения с учетом типа медиа

**Пользовательский опыт:**
- Аудио файлы защищены от дублирования с информативными сообщениями
- Изображения и видео свободно заменяют друг друга
- Интуитивное поведение: аудио не дублируется, визуальный контент заменяется

### [2024-12-19] - Исправление логики замены типов медиа

**Исправленная логика воспроизведения:**
- Все типы медиа совместимы и могут заменять друг друга
- Одинаковые типы медиа заменяют друг друга (не блокируются):
  - ✅ Звук заменяет звук (новое аудио останавливает предыдущее)
  - ✅ Видео заменяет видео (новое видео останавливает предыдущее)
  - ✅ Картинка заменяет картинку (новое изображение заменяет предыдущее)
- ✅ Разрешены комбинации разных типов:
  - Звук + картинка (параллельно)
  - Звук + видео (параллельно)
  - Видео + аудио (через триггеры)

**Техническая реализация:**
- Метод `IsMediaTypeCompatible()` всегда возвращает true (все типы совместимы)
- Метод `GetCurrentMediaType()` для определения текущего типа воспроизведения
- Убраны блокирующие проверки - теперь медиа заменяют друг друга
- Логика работает как для отдельных слотов, так и для триггеров

**Пользовательский опыт:**
- Любой тип медиа может заменить любой другой тип
- Нет блокирующих сообщений - все работает интуитивно
- Предотвращение багов через правильную замену контента

### [2024-12-19] - Реализация селективного параллельного воспроизведения

**Новая логика воспроизведения:**
- Аудио слоты не заменяют визуальный контент (изображения/видео)
- Визуальный контент (видео/изображения) заменяет только визуальный контент
- Аудио заменяет только аудио, сохраняя изображения на экране
- Возможность параллельного воспроизведения: аудио из одного слота + изображение из другого

**Система отслеживания по типам медиа:**
- _currentVisualContent: отслеживает видео/изображения в основном плеере  
- _currentAudioContent: отслеживает активное аудио
- _activeAudioSlots: словарь всех активных аудио элементов
- Селективная остановка: StopActiveAudio() vs StopActiveTriggersAndVisual()

**Примеры использования:**
1. Запустили триггер (картинка + звук) → затем кликнули на аудио слот → картинка остается, звук заменяется
2. Запустили аудио в слоте 1 → кликнули на изображение в слоте 2 → аудио продолжает играть с новым изображением  
3. Запустили видео → оно заменяет и визуальный контент, и аудио (полная замена)

### [2024-12-19] - Кардинальное исправление системы управления медиа

**Полностью переработано:**
- Система отслеживания владельца основного медиаплеера (_currentMainMedia)
- Правильная остановка предыдущих триггеров - теперь они действительно останавливаются
- Независимая работа отдельных слотов в любое время
- Упрощенная логика основных кнопок управления без путаницы с "последним триггером"

**Новая архитектура:**
- _currentMainMedia отслеживает кому принадлежит основной плеер
- StopParallelMedia останавливает медиа только если оно принадлежит конкретному триггеру
- StartParallelMedia останавливает ВСЕ другие активные триггеры перед запуском
- Отдельные слоты получают приоритет и могут запускаться в любой момент

**Для организации мероприятий:**
- Гарантированная остановка предыдущего контента при запуске нового
- Независимое управление отдельными медиафайлами
- Четкое разделение между триггерами и отдельными слотами
- Надежная работа основных кнопок управления

### [2024-12-19] - Исправление логики управления триггерами

**Исправлено:**
- Ограничение одновременного запуска триггеров - только один может быть активным
- Основные кнопки управления (Воспроизвести/Остановить/Закрыть) теперь корректно работают с триггерами
- Отдельные слоты работают после остановки триггеров
- Автоматическая остановка других триггеров при запуске нового
- Запоминание последнего использованного триггера для основных кнопок

**Логика работы:**
- При запуске нового триггера - все остальные автоматически останавливаются
- Основные кнопки управляют активным триггером, если есть
- Если нет активного триггера, кнопки управляют последним использованным
- Клик по отдельному слоту останавливает все триггеры
- Кнопка "Загрузить" также останавливает активные триггеры

**Техническая реализация:**
- Добавлено отслеживание _lastUsedTriggerColumn
- Улучшена логика StartParallelMedia с проверкой других активных триггеров
- Обновлены все основные кнопки для работы с приоритетом триггеров
- Slot_Click и LoadMedia останавливают триггеры перед своим действием

### [2024-12-19] - Исправление проблем с триггерами

**Исправлено:**
- Проблема с отображением изображений в связке аудио+картинка
- Конфликт между основными кнопками управления (Воспроизвести/Остановить/Закрыть) и триггерами
- Основные кнопки теперь работают с активными триггерами
- MediaElement корректно заменяется на Image для показа статических изображений
- Правильное восстановление MediaElement после окончания воспроизведения

**Техническая реализация:**
- Добавлен Image элемент для отображения статических изображений
- Отслеживание активного триггера через _activeTriggerColumn
- Интеграция основных кнопок управления с системой триггеров
- Методы FindMediaBorder() и FindVisualChild() для работы с визуальным деревом
- Корректная замена Child элементов в Border контейнере

### [2024-12-19] - Улучшенное управление кнопками-триггерами

**Добавлено:**
- Система состояний для кнопок-триггеров (Stopped, Playing, Paused)
- Визуальная индикация состояния: ▶ (оранжевый), ⏹ (красный), ⏸ (желтый)
- Функция паузы/возобновления при повторном нажатии на активный триггер
- Автоматическое завершение воспроизведения при окончании медиафайлов
- Отслеживание активных MediaElement и временных контейнеров
- Правильная очистка ресурсов при остановке воспроизведения
- Синхронизированное управление основным плеером и дополнительными аудио элементами

**Техническая реализация:**
- Словари для отслеживания состояний: _triggerStates, _activeAudioElements, _tempContainers
- Подписка на события MediaEnded для автоматической остановки
- Методы StartParallelMedia, PauseParallelMedia, ResumeParallelMedia, StopParallelMedia
- Улучшенная функция ClearAllSlots с остановкой всех воспроизведений

### [2024-12-19] - Добавление кнопок-триггеров для параллельного воспроизведения

**Добавлено:**
- Третья строка кнопок-триггеров под каждой колонкой (10 кнопок)
- Кнопки-триггеры размером 35x35 px (в 2 раза меньше основных слотов)
- Оранжевый цвет с символом ▶ для триггеров
- Функция параллельного воспроизведения медиа из строк 1 и 2:
  - Видео + Аудио: видео в основном плеере + аудио параллельно
  - Изображение + Аудио: изображение в плеере + аудио параллельно
- Автоматическое создание временных MediaElement для аудио
- Синхронизированное воспроизведение нескольких медиа источников
- Обновленная модель данных с полем IsTrigger

**Техническая реализация:**
- Обновлена структура Grid с 3 строками вместо 2
- Добавлены стили TriggerButtonStyle для кнопок-триггеров
- Новые методы PlayParallelMedia, PlayVideoWithAudio, PlayImageWithAudio
- Временные контейнеры для скрытых аудио элементов

### [2024-12-19] - Реализация системы проектов и интерактивных слотов

**Добавлено:**
- Меню File с функциями New Project, Open Project, Save Project, Exit
- Система проектов с сохранением в JSON формате
- Модели данных ProjectModel, MediaSlot, MediaType
- ProjectManager для работы с JSON файлами проектов
- PreviewGenerator для создания превью медиафайлов
- Интерактивные слоты (20 кнопок 10x2) для быстрого доступа к медиа
- Поддержка видео, изображений и аудиофайлов
- Визуальные иконки для разных типов медиа (🎥🖼️🎵)
- Автоматическое воспроизведение медиа при клике на слот
- Загрузка медиафайлов в слоты через диалог выбора файлов

**Структура проекта:**
- Models/ProjectModel.cs - модели данных
- Services/ProjectManager.cs - управление проектами
- Services/PreviewGenerator.cs - генерация превью

**Исправления:**
- Исправлены ошибки компиляции с nullable reference types
- Добавлены необходимые using директивы (System.IO, System.Linq)
- Обновлены сигнатуры методов для корректной работы с nullable типами

### [2024-12-19] - Добавление черного фона для MediaElement

**Добавлено:**
- MediaElement обернут в Border с черным фоном (Background="Black")
- Теперь плеер отображается как черный экран до загрузки видео, вместо отсутствия визуального элемента
- Исправлена ошибка MC3072: свойство "Background" не существует для MediaElement
- Исправлено несоответствие размеров: MediaElement теперь имеет те же размеры, что и Border (600x400)
- Добавлено свойство Stretch="UniformToFill" для устранения черных полос при воспроизведении видео

### [2024-12-19] - Исправление ошибки компиляции MediaElement

**Исправлено:**
- Добавлен атрибут `Name="mediaElement"` к MediaElement в MainWindow.xaml
- Обновлены все ссылки в коде с `MediaElement.` на `mediaElement.`
- Добавлен using `Microsoft.Win32` для OpenFileDialog
- Исправлена ошибка CS0120: "Для нестатического поля, метода или свойства требуется ссылка на объект"

**Детали:**
Проблема заключалась в том, что MediaElement в XAML не имел имени, поэтому код пытался обращаться к нему как к статическому классу. Теперь MediaElement правильно именован и доступен в code-behind как экземпляр объекта.

## Разработка

### Требования
- .NET 8.0 SDK
- Visual Studio 2022 или VS Code (рекомендуется)

### Сборка
```bash
dotnet build
```

### Запуск в режиме отладки
```bash
dotnet run
```

## Сборка для распространения

### Self-contained deployment
Для создания портативной версии приложения, которую можно запустить на любом компьютере без установки .NET:

```bash
# Перейти в папку проекта
cd ArenaApp

# Создать self-contained сборку для Windows x64
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=false -p:PublishTrimmed=false -o ../ArenaApp_Publish
```

**Результат:**
- Папка `ArenaApp_Publish` содержит все необходимые файлы
- Размер: ~150-200 MB (включает .NET runtime)
- Запуск: `ArenaApp.exe` из папки
- Не требует установки .NET на целевом компьютере

### Альтернативные варианты сборки

**Для других архитектур:**
```bash
# Windows x86 (32-bit)
dotnet publish -c Release -r win-x86 --self-contained true -o ../ArenaApp_Publish_x86

# Windows ARM64
dotnet publish -c Release -r win-arm64 --self-contained true -o ../ArenaApp_Publish_ARM64
```

**Framework-dependent (требует .NET 8.0 на целевом компьютере):**
```bash
# Меньший размер, но требует .NET 8.0
dotnet publish -c Release -r win-x64 --self-contained false -o ../ArenaApp_Publish_Framework
```

## Changelog

### [2024-12-19] - Исправление краша второго экрана при зацикливании

**Исправленные баги:**
- **Исправлен краш второго экрана при зацикливании**: Добавлены проверки на существование окна и элемента перед операциями с вторым экраном. Теперь при зацикливании видео второй экран корректно перезапускается без краша
- **Добавлены проверки состояния**: Перед любыми операциями с `_secondaryMediaElement` теперь проверяется, что `_secondaryScreenWindow` существует и `_secondaryMediaElement` все еще является содержимым окна

**Технические детали:**
- Добавлена проверка `_secondaryScreenWindow.Content == _secondaryMediaElement` перед операциями
- Добавлены проверки на null для всех операций с вторым экраном
- Ошибки теперь логируются, но не крашат приложение

**Объяснение размера файла:**
- Создан документ `REFACTORING_EXPLANATION.md` с подробным объяснением, почему `MainWindow.xaml.cs` не уменьшается
- Кратко: мы создали **инфраструктуру** (сервисы, обертки, делегаты), но **не перенесли логику** из MainWindow
- Вся основная логика (7000+ строк) все еще находится в MainWindow
- Следующий этап - перенос логики в сервисы

### [2024-12-19] - Исправление краша при паузе аудио

**Исправленные баги:**
- **Исправлен краш при паузе аудио**: Заменены все небезопасные прямые обращения к `_activeAudioSlots[key]` на безопасные проверки через `TryGetValue`. Теперь при попытке поставить аудио на паузу приложение не крашится, даже если элемент не найден в словаре
- **Использование MediaStateService**: Все обращения к аудио слотам теперь используют методы `TryGetAudioSlot`, `AddAudioSlot`, `RemoveAudioSlot` из MediaStateService для безопасной работы
- **Добавлены проверки на null**: Добавлены проверки на null перед вызовом методов на аудио элементах

**Технические детали:**
- Заменены все прямые обращения `_activeAudioSlots[key]` на `_mediaStateService.TryGetAudioSlot(key, out var element)`
- Добавлены проверки на null перед вызовом методов `Play()`, `Pause()`, `Stop()` на аудио элементах
- Исправлены места в `Slot_Click`, `PauseItem_Click`, `StopMedia`, `PlayMedia` и других методах

### [2024-12-19] - Исправление багов синхронизации паузы и зацикливания

**Исправленные баги:**
- **Исправлена синхронизация паузы со вторым экраном**: При паузе видео теперь синхронизируется со вторым экраном. Второй экран больше не показывает зацикленный кадр остановки, а корректно ставится на паузу вместе с основным экраном
- **Исправлено зацикливание на втором экране**: При включенном зацикливании и окончании видео второй экран теперь перезапускается с начала вместе с основным экраном, а не закрывается
- **Добавлена синхронизация перезапуска**: При перезапуске видео (кнопка "Заново" или зацикливание) второй экран также перезапускается с начала

**Технические детали:**
- Созданы вспомогательные методы `SyncPauseWithSecondaryScreen()` и `SyncPlayWithSecondaryScreen()` для централизованной синхронизации
- Добавлена синхронизация паузы/возобновления во всех местах, где вызывается `mediaElement.Pause()` и `mediaElement.Play()`
- Исправлен обработчик `MediaEnded` для второго экрана - теперь он проверяет настройку зацикливания и перезапускает видео вместо остановки
- Добавлена синхронизация перезапуска в метод `RestartItem_Click`

### [2024-12-19] - Исправление багов синхронизации второго экрана

**Исправленные баги:**
- **Исправлена синхронизация позиции при перемотке**: При перемотке видео через слайдер теперь позиция синхронизируется со вторым экраном. Видео на втором экране больше не зависает при перемотке
- **Исправлено вылетание второго экрана при окончании видео**: Добавлен обработчик `MediaEnded` для `_secondaryMediaElement`, который корректно обрабатывает окончание видео и предотвращает ошибки. Также добавлена синхронизация остановки при окончании видео на основном экране
- **Добавлена синхронизация позиции в реальном времени**: Позиция видео синхронизируется между основным и вторым экраном в реальном времени через таймер (с порогом 0.1 секунды для оптимизации)

**Технические детали:**
- Синхронизация позиции при перемотке добавлена в `VideoSlider_PreviewMouseLeftButtonUp`
- Обработчик `MediaEnded` для второго экрана добавлен при создании `_secondaryMediaElement`
- Синхронизация в реальном времени добавлена в обработчик `VideoTimerUpdated` с защитой от ошибок

### [2024-12-19] - Продолжение рефакторинга: Создание новых сервисов

**Созданные сервисы:**
- **MediaPlayerService**: Управление воспроизведением медиа (обновление MediaElement, создание Image элементов, синхронизация со вторым экраном, остановка/пауза/возобновление)
- **TextBlockService**: Управление текстовыми блоками (создание, обновление, удаление, синхронизация со вторым экраном, применение настроек)
- **TriggerManager**: Управление триггерами (состояния, получение слотов, определение типов медиа, остановка триггеров)
- **TriggerState enum**: Перенесен в отдельный файл `Models/TriggerState.cs` для лучшей организации кода

**Интеграция сервисов:**
- Добавлена инициализация новых сервисов в `InitializeServices()`
- Созданы методы-обертки для обратной совместимости с существующим кодом
- TriggerManager интегрирован с синхронизацией состояния через методы `GetTriggerState` и `SetTriggerState`

**Технические детали:**
- Все сервисы используют делегаты для доступа к UI элементам, сохраняя разделение ответственности
- Сохранена обратная совместимость - старый код продолжает работать
- Код компилируется без ошибок

### [2024-12-19] - Исправление багов и продолжение рефакторинга

**Исправленные баги:**
- **Исправлено растягивание медиа в мини проигрывателе**: Изменен Stretch с UniformToFill на Uniform в XAML и в методе UpdateMediaElement. Теперь все медиа правильно помещаются в зону проигрывателя, а не растягиваются на весь экран
- **Исправлен вывод на второй экран для видео**: Добавлена проверка и создание _secondaryMediaElement если он не существует. Добавлен обработчик MediaOpened для автоматического запуска воспроизведения после загрузки видео на втором экране
- **Исправлено применение поворота на втором экране**: Добавлено применение ApplyScaleAndRotation к _secondaryMediaElement во всех местах, где применяются настройки к видео на втором экране. Теперь поворот, масштаб и прозрачность корректно синхронизируются с основным экраном

### [2024-12-19] - Начало рефакторинга: Разбиение MainWindow.xaml.cs на сервисы

**Рефакторинг архитектуры:**
- **Созданы сервисы для разделения ответственности**: Начато разбиение большого файла MainWindow.xaml.cs (6763 строки) на отдельные сервисы для улучшения читаемости и поддержки кода
- **TimerService**: Управление таймерами видео и аудио, обновление слайдеров перемотки
- **MediaStateService**: Управление состоянием медиа (активные слоты, позиции воспроизведения, пауза, отслеживание активных файлов)
- **DeviceManager**: Работа с экранами и аудиоустройствами
- **TransitionService**: Управление переходами между медиа (затухание, скольжение, зум)
- **SettingsManager**: Управление настройками элементов и общими настройками проекта
- **SlotManager**: Управление слотами медиа, парсинг ключей, поиск элементов
- **PanelDragService**: Управление перетаскиванием и изменением размера панелей
- **Интеграция сервисов**: Начата интеграция TimerService и MediaStateService в MainWindow с сохранением обратной совместимости
- **Постепенный рефакторинг**: Рефакторинг выполняется постепенно для сохранения стабильности приложения

**Технические детали:**
- Сервисы используют делегаты и события для доступа к UI элементам, сохраняя разделение ответственности
- Добавлены свойства-обертки для обратной совместимости со старым кодом
- Все существующие функции сохранены и работают как прежде
- Рефакторинг продолжается - планируется дальнейшее разбиение на модули

### 2024-12-19 (Финальная версия) - Полностью перетаскиваемый интерфейс с улучшенным UX
- **Перетаскиваемые панели настроек**: Панели настроек элемента и общих настроек теперь можно перетаскивать по экрану
- **Многонаправленные маяки**: Добавлены маяки для вертикального, горизонтального и диагонального изменения размера всех панелей
- **Перетаскиваемый медиаплеер**: Медиаплеер с таймерами и слайдерами теперь можно перетаскивать и изменять размер
- **Перетаскиваемая панель медиа-клеток**: Панель с медиа-клетками (создаваемыми циклом) теперь перетаскиваемая и растягиваемая
- **Встроенные панели**: Все панели теперь встроены в основное окно, но с полной функциональностью перетаскивания
- **Наложение панелей**: Все панели можно располагать поверх друг друга и основного контента
- **Система "зажать и тянуть"**: Маяки активируются только при зажатии и перетаскивании, что исключает случайные срабатывания
- **Блокировка перетаскивания при растягивании**: При активации растягивания автоматически отключается перетаскивание панели
- **Визуальные маяки**: Маяки теперь видимые (синие полоски) с подсветкой при наведении (opacity 0.7 → 1.0)
- **Адаптивный медиаплеер**: Медиаплеер теперь действительно растягивается вместе с панелью
- **Стабильная работа**: Устранены баги и зависания при растягивании панелей
- **Сохранение позиций панелей**: Позиции и размеры всех панелей сохраняются в JSON файле проекта
- **Восстановление позиций**: При загрузке проекта панели восстанавливаются в тех же позициях и размерах
- **Автосохранение при закрытии**: Позиции панелей автоматически сохраняются при закрытии приложения
- **Исправлена логика зацикливания плейлиста**: При включенной галочке "зацикливание плейлиста" медиа перезапускается с начала (использует метод restart)
- **Исправлено автопереключение**: При достижении последнего элемента в строке переходит к первому элементу
- **Приоритет зацикливания**: Зацикливание плейлиста имеет приоритет над автопереключением
- **Исправлены функции кнопок навигации**: Верхние кнопки (◀/▶) теперь переключают настройки элементов, нижние кнопки - медиа
- **Исправлена логика переключения медиа**: Теперь переключение происходит по строкам (1→2→3→...→19→1), а не по столбцам
- **Закрытие окна вывода на второй монитор**: При закрытии приложения теперь корректно закрывается окно вывода медиа на второй монитор
- **Логика кнопок навигации**: Кнопки "◀" и "▶" в настройках элемента теперь переключают медиа (следующее/предыдущее), а не окно настроек

### 2024-12-19 (Продолжение)
- **Исправлена перемотка видео в реальном времени**: Добавлена переменная `_isVideoSliderDragging` и обработчики событий `PreviewMouseLeftButtonDown/Up` для видео слайдера. Теперь видео реагирует на перетаскивание слайдера в реальном времени, а не "догоняется" после отпускания. VideoTimer_Tick не обновляет слайдер во время перетаскивания пользователем, предотвращая конфликты.
- **Исправлен таймер аудио в триггерах**: При использовании существующего MediaElement из слота в триггере, элемент теперь правильно регистрируется в `_activeAudioSlots` с ключом триггера. Это позволяет таймеру аудио корректно отображать время воспроизведения вместо 0:00.
- **Исправлена подсветка кнопок триггеров**: Убрана ручная установка `Content` и `Background` кнопок триггеров, которая конфликтовала с `UpdateAllSlotButtonsHighlighting()`. Теперь кнопки триггеров правильно отображают состояние: красные при воспроизведении (⏹), желтые при паузе (⏸), оранжевые при остановке (▶).

### 2024-12-19
- **Добавлено контекстное меню для слотов и триггеров**: Реализовано контекстное меню с правой кнопкой мыши для всех кнопок слотов и триггеров. Меню содержит три пункта: "Заново" (перезапуск воспроизведения), "Пауза" (пауза/возобновление) и "Удалить" (удаление элемента, выделен красным цветом). Контекстное меню работает как для отдельных слотов, так и для кнопок-триггеров, предоставляя быстрый доступ к основным функциям управления медиа.
- **Исправлены ошибки компиляции**: Устранены ошибки CS0103, CS7036 и CS8604 в контекстном меню. Исправлены ссылки на `_project` (заменены на `_projectManager.CurrentProject`), корректные вызовы методов `StopMedia()` и `UpdateSlotButton()`, а также обработка nullable ссылок.
- **Исправлена логика контекстного меню**: Улучшена логика кнопок "Заново" и "Пауза" в контекстном меню. Теперь они корректно определяют текущее состояние воспроизведения и работают правильно как для слотов, так и для триггеров. Кнопка "Заново" перезапускает воспроизведение, а "Пауза" ставит на паузу или возобновляет воспроизведение в зависимости от текущего состояния.
- **Исправлена логика кликов по слотам**: Клик по кнопке слота теперь ставит на паузу/возобновляет воспроизведение, если слот уже активен, вместо перезапуска. Кнопка "Заново" в контекстном меню теперь действительно перезапускает с самого начала (сбрасывает позицию на 0). Текст кнопки "Пауза" в контекстном меню динамически меняется на "Продолжить" когда медиа на паузе.
- **Исправлена логика паузы и возобновления**: Улучшена логика определения состояния воспроизведения для корректной работы паузы/возобновления. Теперь кнопки "Пауза" в контекстном меню и клики по слотам корректно переключаются между паузой и возобновлением. Исправлена защита от дублирования аудио - теперь она не блокирует паузу/возобновление того же аудио файла в том же слоте.
- **Исправлено сохранение позиции видео**: Добавлено сохранение позиции видео при переключении на картинку и обратно. Теперь при переключении с видео на картинку, а затем обратно на видео, воспроизведение продолжается с того места, где было остановлено, а не с самого начала.
- **Добавлены независимые таймеры для видео и аудио**: Реализованы два отдельных таймера - "Видео: HH:MM:SS" и "Аудио: HH:MM:SS". Таймеры всегда активны и показывают оставшееся время воспроизведения соответствующих медиафайлов. Видео и аудио таймеры работают независимо друг от друга, что позволяет отслеживать время для каждого типа медиа отдельно.
- **Исправлена совместимость видео и аудио**: Теперь при запуске видео аудио продолжает играть, а не останавливается. Это позволяет воспроизводить видео с фоновой музыкой или звуковыми эффектами.
- **Добавлено запоминание позиций для каждого слота**: Каждый слот (включая триггеры) запоминает позицию воспроизведения в памяти приложения. При переключении между слотами и возврате к предыдущему слоту, воспроизведение продолжается с того места, где было остановлено. Позиции не сохраняются в файл проекта и сбрасываются при закрытии приложения.
- **Добавлены инструкции по сборке для распространения**: Добавлен раздел "Сборка для распространения" с подробными инструкциями по созданию self-contained deployment. Теперь можно легко собрать приложение в одну папку для распространения друзьям без необходимости установки .NET.
- **Запоминание позиций воспроизведения**: Добавлена функция сохранения и восстановления позиций воспроизведения для всех медиафайлов. Теперь при переключении между объектами или паузе/возобновлении, медиа продолжает воспроизведение с того места, где было остановлено.
- **Исправление глобальных кнопок управления**: Починены кнопки "Воспроизвести" и "Остановить" в нижней части интерфейса. Теперь они корректно управляют как видео, так и активным аудио, сохраняя и восстанавливая позиции воспроизведения.
- **Правильная замена аудио между слотами и триггерами**: Исправлена логика замены аудио между отдельными слотами и триггерами. Теперь при запуске триггера с новым аудио, аудио из отдельных слотов корректно останавливается, предотвращая параллельное воспроизведение.

## Лицензия

Этот проект создан в образовательных целях.

## Сборка для распространения

### Создание портативной версии

Для создания исполняемого файла, который можно передать другу без установки .NET:

```bash
dotnet publish -c Release -r win-x64 --self-contained true
```

Это создаст папку `bin/Release/net8.0-windows/win-x64/publish/` с готовым к запуску приложением.

### Создание архива

```bash
cd bin/Release/net8.0-windows/win-x64/publish
powershell "Compress-Archive -Path '.' -DestinationPath '../../../../ArenaApp_Portable.zip' -Force"
```

### Что передать другу

1. **Архив `ArenaApp_Portable.zip`** - содержит все необходимые файлы
2. **Инструкция**: Распаковать архив и запустить `ArenaApp.exe`

### Системные требования

- Windows 10/11 (64-bit)
- .NET НЕ требуется (все включено в сборку)

## Контакты

Для вопросов и предложений обращайтесь к разработчику.
